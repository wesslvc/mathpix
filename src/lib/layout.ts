/**
 * 문제 카드(인식 결과 미리보기/수정 미리보기)의 고정 너비(px).
 * 화면 폭에 따라 반응형으로 줄바꿈이 달라지면 같은 문제를 폰/패드에서 저장했을 때
 * 오답프린트에 붙는 결과물의 줄바꿈이 달라져 버린다. 항상
 * 이 너비로 렌더링해 어떤 기기에서 저장하든 같은 모양이 나오게 한다. 화면이 이보다 좋으면 가로 스크롤로 보게 한다(줄바꿈은 그대로 유지).
 */
export const PROBLEM_CARD_WIDTH = 640;
