# 수학오답프린트 제작 — 작업 인수인계

새 세션에서 이 저장소를 열면 이 파일을 먼저 읽고 아래 "남은 작업"부터 이어서 진행하세요.

## 무엇을 만드는 앱인가

수학 오답 사진을 올리면 → 문제 영역을 자동 감지하고 → 사용자가 크롭을 보정하고 →
Mathpix OCR로 인식해서 → 나눔명조 + KaTeX로 가독성 좋게 재구성하고 →
**변환 결과 이미지만** 계정에 저장(원본 사진은 버림) → 실전모의고사(실모)별로 모아뒀다가
→ 출처가 표시된 PDF로 인쇄하는 Next.js 앱.

- 저장소: `wesslvc/mathpix`, 작업 브랜치: `main`
  (한 번 삭제된 적이 있어 커밋 해시로 복구함: `3a92fa19a72ab7b5e8f41d2a47b5a0d547268ae5`)
- **배포는 Vercel의 `mathocr` 프로젝트**를 씁니다(`mathocr-...vercel.app`).
  Vercel 프로젝트 `mathpix`도 같은 저장소에 연결돼 있지만 Production Branch가
  `main`을 따라가지 않아 실사용 배포가 아닙니다. 헷갈리지 말 것.
- Supabase 프로젝트: `bmhupmkxzvbqndxkvjmx` (URL: `https://bmhupmkxzvbqndxkvjmx.supabase.co`).
  이 세션의 Supabase MCP 커넥터는 다른 프로젝트(`seji`, ref `brgvpmpqvqhdjsnrxzhh`)에만
  접근 권한이 있어서 위 프로젝트는 MCP로 직접 조작 불가 — SQL은 사용자가 대시보드에서
  직접 실행해야 함.

## 사용자가 확정한 요구사항

- **오답추가** = 특정 카테고리(실모) 안에 문제를 추가하는 버튼. 독립된 개념이 아님.
- **실모추가** = 출처(예: "2025학년도 6월 모의평가")를 입력해 카테고리를 만드는 것.
- 로그인은 **이메일/비밀번호**. (처음엔 Google OAuth로 만들었으나 사용자가 "구글 버리고
  이메일로만 가능하게 해달라"고 해서 교체함. 다시 Google을 추가해달라고 하지 않는 이상
  이메일/비밀번호 방식 유지.)
- PDF는 **한 페이지에 문제 1개, 하단에 출처 표기**.
- 원본 사진은 저장하지 않고 변환 결과만 저장.

## 이미 끝난 것

- 업로드 → 자동 크롭 감지 → 수동 크롭 보정 → Mathpix 인식 → 결과 렌더링 → PNG 저장
- 결과 렌더링 가독성 재설계: 문단 분리, `>` 블록은 조건 박스, 첫 문단의 문제번호 굵게
- 조건 박스 자동 감지 규칙은 세 가지다(`renderMathTextWithInfo`):
  ① 문단 전체가 `>`로 시작하면 통째로 박스
  ② `(가)`/`(나)` 표지가 있으면 첫 표지부터 마지막 표지 문장의 마침표까지
  ③ `<보기>` 머리글이 있으면 머리글 줄부터 마지막 ㄱ/ㄴ/ㄷ 항목 문장의
     마침표까지 (`BOGI_HEADER` / `BOGI_ITEM`)
  ②와 ③은 한 문제에 같이 나올 수 있고 그때는 박스가 두 개 생긴다.
  ③에서 조심한 것들: **표지의 마침표는 문장의 끝이 아니다** — "ㄷ. 교점은 두
     개이다."에서 첫 마침표(표지의 것)를 문장 끝으로 보면 표지만 박스에 남고
     내용이 박스 밖으로 빠진다. `markerPrefixLength()`로 표지를 건너뛰고
     찾는다(`splitAtSentenceEnd` / `lineHasSentenceEnd` / `closeBoxAtLine`) /
     머리글만 있고 항목이 없으면 박스를 만들지 않는다(머리글만
  갇힌 박스는 박스가 아니다) / Mathpix가 머리글 다음에 빈 줄을 넣어 항목을
  다른 문단으로 보내면 다음 문단까지 이어서 본다 / 질문·선택지 줄을 만나면
  거기서 끊는다 / 마지막 항목에 마침표가 없으면 뒤에서 첫 마침표까지만
  늘리고, 못 찾으면 항목 줄에서 끝낸다(무작정 늘리면 무관한 본문을 삼킨다) /
  같은 문단에서 `(가)`가 `<보기>`보다 앞서면 ③을 적용하지 않고 ②에 맡긴다.
  (`src/lib/renderMathText.ts`, `.mmd-*` 클래스는 `src/app/globals.css`)
- 폰트: 나눔명조 자체 호스팅(`@fontsource/nanum-myeongjo`), 수식은 KaTeX
- 보기 표지(ㄱ/ㄴ/ㄷ): Mathpix가 **조합용 자모**(U+1100~, `ᄀᄂᄃ`)로 주는 경우가
  있는데, 이건 음절 조립용 코드라 홀로 쓰면 위첨자처럼 아주 작게 그려져 눈에
  띄게 깨지고 `BOGI_ITEM`(U+3131~)에도 안 걸려 박스 감지 자체가 안 된다.
  `normalizeJamo()`가 호환용 자모로 바꾼다. **유니코드 NFKC로는 안 된다** —
  표준이 정한 방향이 반대(ㄱ→ᄀ)라서 표를 직접 들고 있다.
  (호환용 자모도 나눔명조에서는 음절보다 작게 그려져서 — 재보니 잉크 높이가
  0.696배 — 한때 1.3배로 키웠지만, 사용자가 1배가 낫다고 해서 되돌렸다.)
- 앱 이름 "수학오답프린트 제작"
- Supabase 연동 코드 전체: **이메일/비밀번호 로그인+회원가입**(`src/app/login/page.tsx`),
  이메일 확인 콜백(`src/app/auth/callback/route.ts`), 미들웨어 인증 가드,
  대시보드(실모 목록), 실모추가 폼, 카테고리 상세(오답추가 + 저장),
  PDF 내보내기(`pdf-lib`)
- DB 스키마 SQL: `supabase/migrations/0001_init.sql`
  (categories / problems 테이블 + RLS + `problem-images` 비공개 버킷)
- 키가 없으면 앱이 죽지 않고 안내 메시지만 표시하도록 처리 (Mathpix mock, Supabase 안내)
- 그림(수학 도형 · 사과탐 자료) 재구성: **하나로 통합됐다.** 예전에는 수학 도형만
  Gemini로 SVG를 만드는 별도 경로가 있었는데 전부 걷어냈다(`diagramVector.ts`,
  `/api/diagram/*`, 플래시쿠폰·모델 티어 전부 삭제). 지금은 과목과 무관하게
  OpenAI 이미지 편집 하나만 쓰고, 과목에 따라 **프롬프트만** 갈린다.

  왜 걷어냈나: 두 경로를 유지할 이유가 없었고(품질도 이미지 생성이 낫다),
  Gemini 쪽은 무료 등급 RPD 때문에 티어 캐스케이드·플래시쿠폰·전역 예산 같은
  장치가 잔뜩 붙어 있어 유지 비용이 컸다. 0010~0014 마이그레이션이 만든 DB
  함수들(`consume_diagram_credit`, `diagram_model_tiers` 등)은 이제 아무도
  부르지 않는다 — 그냥 남겨둬도 무해하다.

### 브라우저를 닫아도 결과는 남는다 (문제 전체 그리기)

큐는 **브라우저 안에서** 돈다. 그래서 탭을 닫거나(모바일에서 앱을 바꾸면 흔하다)
다른 화면으로 가면 fetch가 끊기는데, 토큰은 이미 서버에서 차감된 뒤다 —
그대로 두면 **돈만 나가고 아무것도 안 남는다.** 두 겹으로 막는다.

1. **큐를 루트 레이아웃에 둔다**(`src/app/layout.tsx`). 예전에는 실모 페이지
   안에 있어서 목록으로 돌아가는 순간 통째로 사라졌다. 이제 앱 안에서 어디로
   가든 작업이 계속 돌고 진행 패널도 따라다닌다.
2. **문제 전체 그리기는 서버가 결과를 직접 저장한다**(`persistWholeProblem`).
   브라우저가 죽어도 서버 함수는 끝까지 돌므로, 생성한 이미지를 스토리지에
   올리고 `image_path`와 `box_range.figures`를 갱신한다. 화면이 살아 있으면
   화면이 더 예쁜 카드로 다시 저장하니, 이건 "최소한 남기는" 보험이다.
   저장에 실패해도 요청은 성공으로 돌려준다 — 이미 만든 그림을 버릴 이유가 없다.

**그림 하나(figure) 모드는 서버가 저장하지 못한다.** 그건 본문과 합쳐 카드를
다시 그려야 하는데 그건 브라우저(html-to-image)에서만 된다. 문제 전체는 그림
한 장이 곧 카드라서 서버만으로 가능하다.

**작업을 넣는 건 기다리지 않는다.** 한때 "저장 → 그 id로 큐에 넣기" 순서로
했는데, 저장 왕복이 끝나기 전에 사용자가 다음 사진으로 넘어가면 화면이
사라지면서 **작업이 아예 안 들어갔다.** 지금은 곧바로 넣고 저장은 따로 걸며,
실제 행 id는 일꾼이 호출 직전에 `snapshotsRef`(화면이 계속 갱신해 준다)에서
읽는다.

### AI 그림 작업은 화면 바깥에서 돈다 (FigureJobsProvider)

AI 그림 생성은 수십 초 걸린다. 그동안 **사용자는 다음 문제를 계속 처리할 수
있어야 한다.** 그래서 큐를 `ResultStage` 안이 아니라 페이지 수준
(`FigureJobsProvider`)에 둔다 — 화면 안에 두면 다음 이미지로 넘어가는 순간
작업이 통째로 사라진다.

동작 순서:
1. 자료를 오려내면 **원본이 곧바로 카드에 붙는다**(빈 자리가 생기지 않는다).
2. AI 작업은 큐에 들어가 한 번에 하나씩 순서대로 돈다.
3. 진행 상황은 화면 구석의 `FigureJobsPanel`에서 본다(문제 화면과 무관하게 떠 있다).
4. 완성되면
   - 그 문제 화면이 아직 열려 있으면 → 화면이 미리보기를 갈아끼우고 dirty로
     표시해 자동 저장이 다시 돈다.
   - 이미 닫혔으면 → Provider가 `renderCardOffscreen()`으로 **눈에 안 보이는
     곳에 카드를 다시 그려** 저장된 이미지를 갈아끼운다.

이게 가능하려면 화면이 없어도 카드를 다시 그릴 수 있어야 한다. 그래서
`ResultStage`는 살아 있는 동안 `putSnapshot()`으로 최신 상태(본문·박스·글자
크기·그림 목록과 위치)를 Provider에 계속 넘겨둔다.

**카드 조립은 `src/lib/cardHtml.ts` 한 곳에만 있다.** 화면과 뒷단이 같은 결과를
내야 해서다 — 양쪽에 따로 구현하면 반드시 어긋난다. `buildAnchors()`와
`buildCardHtml()`은 정확히 같은 순서로 훑어야 하고, 드래그의 `anchorPoints()`도
마찬가지다.

화면 밖 캡처가 화면 안 캡처와 같은지는 실제 브라우저로 확인했다 — 크기
1520×928, 도형 픽셀 수, 글자 잉크량이 **완전히 일치**한다. `display:none`이나
`visibility:hidden`은 쓰면 안 된다(레이아웃이 계산되지 않아 캡처가 빈다).
화면 밖으로 밀어내는 방식만 쓴다.

#### 큐가 멈추던 자리 (다시 건드릴 사람에게)

일꾼 이펙트가 **`jobs`만 바라보면 큐가 조용히 멈춘다.** 작업이 끝날 때 순서가
이렇다: `setJobs(완료)` → React 가 다시 그림 → 일꾼 이펙트가 도는데 이때
`runningRef` 는 **아직 비워지기 전**이라 그냥 돌아간다 → 그 뒤에 `runningRef`
가 비워지지만 더 이상 상태가 바뀌지 않아 이펙트가 다시 돌지 않는다. 대기 중인
작업이 있어도 아무도 집어가지 않는다.

**저장본 갱신을 기다릴 때 특히 잘 터진다.** `resaveIfClosed` 는 이미지를 올리고
DB 를 고치느라 몇 초씩 걸리는데, 그 사이에 React 가 렌더를 끝내 버리기 때문이다.
즉 **앞 문제 화면을 닫고 다음 문제로 넘어간 뒤에** 멈춘다 — 여러 개를 쌓아 두는
바로 그 상황이다.

두 가지로 막는다: ① `runningRef` 를 비운 **다음에** `wake` 를 올려 확실히
깨운다, ② 저장본 갱신은 기다리지 않고 뒤로 보낸다(다음 작업을 막지 않는다).

실제 브라우저(개발 서버 + 가짜 `/api/figure`)로 재현하고 확인했다 — 고치기
전에는 세 개를 넣으면 **첫 번째만 done, 나머지는 pending 으로 멈췄고**, 고친
뒤에는 셋 다 연달아 처리된다.

### 도형·자료를 본문 문단 사이에 놓기

예전에는 도형·자료가 무조건 본문 **맨 아래**에 붙었다. 문제집은 자료가 문장
사이에 오는 경우가 많아서 사용자가 위치를 고를 수 있어야 한다.

`renderMathTextWithInfo`가 `blocks: string[]`(최상위 요소 하나씩)을 함께
돌려주고, `ResultStage`가 그 사이사이에 도형 HTML을 끼워 **하나의 문자열로**
합쳐 카드에 넣는다(`cardHtml`). 위치는 `figurePos[id]` = "이 문단 앞"이고
0이 맨 위, `blocks.length`가 맨 아래(기본값)다.

**React 요소로 따로 두지 않고 문자열로 합치는 이유**: 문단마다 감싸는
`<div>`가 생기면 모든 `<p>`가 각자 `:last-child`가 되어
`.mmd-paragraph:last-child { margin-bottom: 0 }`이 전부 걸려버리고 문단 간격이
통째로 사라진다. 실제 브라우저에서 확인한 값 — 문자열로 합치면 중간 문단
22px 유지, 마지막만 0px.

도형에 붙는 클래스가 Tailwind 유틸리티가 아니라 `.problem-figure`(globals.css)인
것도 같은 이유다. HTML 문자열에는 클래스명만 적을 수 있다.

**미리보기에서 그림을 손으로 끌어 옮긴다.** 처음엔 ▲▼ 버튼으로 만들었다가
불편하다고 해서 드래그로 바꿨다. 조절 패널의 목록은 지금 어디에 놓였는지
보여주는 표시 겸 보조 수단으로만 남겼다.

구현에서 조심할 점 — **끄는 동안에는 React state를 건드리지 않는다.** 본문은
`dangerouslySetInnerHTML` 한 덩어리라, state가 바뀌어 `cardHtml` 문자열이
달라지면 React가 innerHTML을 통째로 갈아치우고 지금 잡고 있는 그 DOM 요소가
사라진다. 그러면 포인터 캡처가 끊겨 드래그가 중간에 죽는다. 끄는 동안에는
`drag.el.style.marginLeft`만 직접 바꾸고, 최종 위치는 손을 뗄 때 한 번만
state에 반영한다. 안내선 위치는 state로 두어도 되는데, `cardHtml`의 의존성이
아니라 문자열이 그대로여서 React가 DOM을 손대지 않기 때문이다.

터치가 스크롤로 먹히지 않게 `.problem-figure`에 `touch-action: none`이 필요하다.
안내선은 `cardRef` **바깥**(감싸는 relative 껍데기)에 둔다 — 안에 두면 PNG
캡처에 파란 줄이 같이 찍힌다. 자동 저장도 드래그 중에는 미룬다(끄는 중에는
DOM만 바뀐 상태라 지금 캡처하면 어중간한 위치가 이미지로 굳는다).

실제 포인터 입력(Playwright, 마우스 + 모바일 터치)으로 15가지를 확인했다.

### 과목 구분은 없앴다

한때 실모별로 "수학 / 사과탐"을 고르게 했다. 그런데 통합 뒤 남은 차이가
프롬프트와 화면 문구뿐이었고, 한 실모 안에 도형과 자료가 섞이면 오히려
어긋났다. 지금은 구분이 없다 — 프롬프트 하나에 항목을 **조건부로** 적어
("도형이나 그래프라면…", "장치도·모식도라면…") 그림 종류에 맞는 지시만
적용되게 했다. `src/lib/subject.ts`는 삭제됐다.

### 지면 한 장을 문제 여러 개로 잘라 넣기

문제가 여러 개 있는 지면(모의고사 한 쪽)을 올려 문제마다 잘라 내고, "모두
AI로 재생성"을 누르면 잘린 것들이 전부 문제 전체 다시 그리기 큐에 들어간다
(`BatchSplitPanel`).

자리를 정하는 방법이 **두 가지**다.

**① 손으로 네모 그리기 — 누구나 쓴다.** 사진 위에 문제마다 네모를 끌어 그리면
그 자리대로 자른다. 모델을 부르지 않으므로 공짜고, 자리를 사람이 정하니 틀릴
일이 없다.

- **그린 차례가 곧 문제 차례다.** 자동으로 찾을 때와 다른 점이다 — 그쪽은
  모델이 순서를 안 지켜서 우리가 단·위치로 정렬하지만, 사람은 읽는 순서대로
  그리므로 그 순서가 곧 답이다. 번호가 네모 위에 바로 찍히니 틀리면 눈에
  띄고, 순서는 나중에 목록에서 얼마든지 바꿀 수 있다(`OrderInput`).
- **손으로 그린 네모에는 여유(`PAD`)를 주지 않는다.** 사용자가 정한 자리라
  우리가 몰래 넓히면 보이는 것과 잘리는 것이 달라진다.
- **끌기는 window 에서 받는다**(`setPointerCapture` 아님). 사진 **바깥**에서
  손을 놓는 일이 흔한데(가장자리 문제를 그릴 때) 그때 pointerup 이 아무 데도
  닿지 않으면 그린 게 통째로 사라진다. 좌표는 화면 밖으로 나가면 0~1 로
  자른다 — 브라우저로 확인했다(0.05~바깥으로 끌면 폭이 1200×0.95=1140px).
- **state 갱신 함수 안에서 다른 state 를 바꾸면 안 된다.** `setDraft(d => {
  setBoxes(...) })` 로 썼다가 **끌기 한 번에 네모가 두 개** 생겼다(갱신
  함수는 순수해야 하고 React 가 두 번 부른다). 지금은 `draftRef` 로 최신
  값을 들고 있다가 손을 뗄 때 읽는다.
- 터치가 스크롤로 먹히지 않게 `touch-action: none` 이 필요하다.
- 네모의 `×` 는 `pointerdown` 을 멈춰야 한다 — 안 그러면 지우려고 누른 것이
  새 네모를 그리기 시작한다.

**잘라 놓은 조각은 손으로 다시 합칠 수 있다.** 자동으로 찾은 결과가 한 문제를
둘로 쪼개 놓는 일이 있고(발문과 선지가 따로 잡히거나, 단을 넘어간 문제를 못
묶거나), 손으로 그릴 때도 나눠 그렸다가 합치고 싶을 수 있다. 조각 카드를 눌러
고르고 "고른 것 합치기"를 누른다.

- **그림을 이어 붙이는 게 아니라 원본에서 다시 자른다.** 그래서 `Piece` 가
  자른 자리(`boxes`)와 여유(`pad`)를 들고 있다. 이미 잘라 둔 그림을 붙이면
  같은 단에 있던 것도 이음매가 생긴다.
- 자리를 다루는 규칙은 `src/lib/problemBoxes.ts` 에 모아 서버와 화면이 나눠
  쓴다. 이 파일에는 네트워크 호출도 환경변수도 없다 — 브라우저에도 실려야
  하기 때문이다.
- **다만 붙이는 함수는 둘이고, 서로 달라야 한다.**
  `mergeWithinColumn`(서버)은 **이미 한 문제로 판정된** 조각에 쓴다. 그것들은
  서로 붙어 있는 게 보장돼 있어(`ADJACENT` 로 걸러 묶었다) 같은 단이면 통째로
  아울러도 안전하다. `mergeChosen`(화면)에는 그 보장이 없다 — 사용자는 아무거나
  고를 수 있다. **사이에 다른 문제가 낀 둘을 고르면 통째로 아우르는 순간 그
  문제까지 딸려 들어와 지면이 통째로 한 조각이 되고, 그 문제는 목록에도 따로
  남아 두 번 나온다.** 실제로 그렇게 됐다("갑자기 전체로 퍼진다"). 그래서
  같은 단이라도 **붙어 있는 것끼리만**(`ADJACENT` 이내) 아우르고 멀리 떨어진
  것은 세로로 이어 붙인다 — 이어 붙이기는 조각만 가져오므로 사이에 있는 것을
  절대 삼키지 않는다.
- **단을 가르는 기준이 서로 다르다.** 서버는 가운데 0.5 로 가르지만
  (`columnOf`) 화면에서는 **조각들이 실제로 가로로 겹치는지**로 가른다
  (`groupByColumn`). 0.5 기준은 두 단짜리 시험지에서 조각이 확실히 한쪽에
  몰려 있을 때 쓰는 것이라 **한 단짜리 지면에서는 통째로 틀린다** — 손으로 그린
  네모는 좌우가 대칭이 아니라 가운데가 0.5 를 아슬아슬하게 넘나들고, 그러면
  위 조각이 "오른쪽 단", 아래 조각이 "왼쪽 단"으로 갈려 **왼쪽 단이 먼저**라는
  규칙 때문에 아래가 위로 올라온다(실제로 뒤집혔다: 위 가운데 0.52, 아래 0.47).
  가로로 반 넘게 겹치면 같은 단으로 본다 — 위아래로 놓인 것은 언제나 겹치고
  나란히 놓인 두 단은 겹치지 않으므로, 한 단짜리든 두 단짜리든 그대로 맞는다.
- **아우르기는 단마다 따로 훑어야 한다.** 앞 단의 마지막 것과 견주면 단을
  넘어선 것끼리 아울러 버려서 두 단을 통째로 감싸는 네모가 된다(좌·우단을
  합쳤더니 1104×1088 백지가 나왔다).
- 결과는 세 갈래다. 브라우저로 확인 —
  **같은 단에서 붙어 있음**: 좌단 위아래를 합치면 504×1088 한 장, 합침 표시
  없음(아우르는 네모로 다시 잘려 이음매가 없다는 뜻).
  **단을 넘어감**: 거기에 우단을 더하면 504×1618 에 "2조각 합침".
  **같은 단인데 멀리 떨어짐**: 사이에 낀 문제를 건너뛰고 1080×1048 로 이어
  붙고(위 초록·아래 파랑), 사이의 빨강은 목록에 그대로 남는다.
- 여유는 고른 것들 중 **가장 큰 것**에 맞춘다(손으로 그린 0 과 자동으로 찾은
  `PAD` 가 섞일 수 있는데, 좁은 쪽에 맞추면 자동 쪽 글자가 잘린다).
- 합친 것은 고른 것들 중 **가장 앞자리**에 놓는다(문제 차례가 유지된다).
- 카드의 `×` 는 `stopPropagation` 이 필요하다 — 안 그러면 지우면서 선택이 켜진다.

**② 자동으로 찾기 — 무제한 계정 전용.** 아래 내용이 전부 이쪽 이야기다
(`/api/detect-problems`). 한 번 누르면 열 몇 문제를 통째로 다시 그리는 일로
이어져 요금이 순식간에 커진다. 화면에서도 감추지만 **막는 자리는 서버**다
(`entitlements.unlimited`) — 화면은 얼마든지 우회할 수 있다.

- **영역 찾기는 Gemini Flash 다.** 그림을 만드는 일이 아니라 **자리를 재는**
  일이라 값이 싸고 빠른 쪽이 맞다. Gemini 가 정해 둔 `box_2d`
  (`[ymin,xmin,ymax,xmax]`, 0~1000 정규화) 규격을 그대로 쓴다.
  모델은 `gemini-flash-latest` 다. 한때 `-lite` 를 썼는데 좌표를 잡는 정확도가
  아쉬워 되돌렸다. 한때 계정의 모델 목록에서 **자동으로 골랐는데 되돌렸다** — 고른
  모델이 이 일에 안 맞으면 왜 실패하는지 알 수 없게 된다. 이름 하나로 못박고
  바꿔야 할 때는 `GEMINI_DETECT_MODEL` 로 바꾼다. 404 면 그 이름을 그대로
  알린다 — 조용히 다른 모델로 갈아타지 않는다(고른 적 없는 모델에 요금이 나간
  적이 있다). `GEMINI_API_KEY` 가 필요하다 — 없으면 이 기능만 안 된다.
- **GPT 로도 견줄 수 있다**(`DETECT_PROVIDER=openai`, 모델은
  `OPENAI_DETECT_MODEL`, 기본 `gpt-5.6-luna`). 한 번 써 봤다가 Gemini 로
  돌아왔다 — 좌표를 재는 일은 Gemini 가 그쪽으로 훈련된 규격 `box_2d` 를 갖고
  있어 유리하다. **응답 형식과 뒤처리(묶기·같은 단 합치기)는 두 갈래가 완전히
  같고** 갈리는 것은 호출 방법뿐이라, 환경변수 하나로 바꿔 가며 결과만 견주면
  된다. 어느 모델이 잡았는지는 화면에도 찍힌다.
- **GPT 는 Responses API 로 먼저 부르고, 안 되면 Chat Completions 로 내려간다.**
  요즘 모델은 Responses 만 받는 경우가 있다. 이건 **같은 모델을 다른 길로**
  부르는 것이라, 고른 적 없는 모델로 갈아타는 것과는 다른 이야기다.
  Responses 는 글을 여러 조각으로 나눠 주므로 `harvest`가 긁어모은다.
- **404 면 이 계정이 가진 gpt 이름들을 함께 알려 준다**(목록 조회는 무료).
  이름이 틀렸을 때 한 번에 고칠 수 있게 하려는 것이지, 비슷한 모델로 몰래
  갈아타려는 게 아니다.
- **영역 찾기와 자르기의 오류를 갈라 놓았다.** 한 덩어리로 감싸 두면 자르다 난
  오류까지 "문제 영역 인식 실패"로 보여서 어디가 잘못됐는지 알 수 없다. 지금은
  자르기 실패면 "영역은 찾았는데 사진을 자르지 못했습니다"로 나오고, 원본을
  못 열면 축소본으로라도 자른다(흐릴지언정 아무것도 못 하는 것보다 낫다).
- **자르기와 다시 그리기를 나눠 두었다.** 잘린 결과를 먼저 눈으로 보고 잘못
  잡힌 것을 `×` 로 지운 다음 돌린다. 영역 인식은 싸지만 다시 그리기는 문제마다
  1분쯤 걸리는 유료 호출이다.
- **저장을 먼저 하고 큐에 넣는다.** 그래야 행 id 를 함께 넘길 수 있어 브라우저를
  닫아도 서버가 결과를 그 행에 직접 저장한다(`persistWholeProblem`). 저장되는
  그림은 우선 원본 크롭이라, 다시 그리기 전에 봐도 빈 자리가 아니다.
- 순서는 **왼쪽 단 위→아래, 그다음 오른쪽 단**으로 우리가 한 번 더 정렬한다
  (모델이 순서를 지키지 않는 경우가 있는데, 자른 차례가 곧 문제 차례다).
- **단을 넘어 이어진 문제는 이어 붙인다.** 모의고사 지면에서는 문제 하나가
  왼쪽 단 아래에서 시작해 오른쪽 단 위로 이어지는 경우가 흔하다. 묶는 방법이
  두 겹이다:
  ① 모델에게 조각마다 **같은 문제 번호**(`no`)를 적게 하고 번호로 묶는다.
  자리로 짐작하지 않는 이유는 두 조각이 지면에서 **가장 멀리 떨어져 있기**
  때문이다(왼쪽 맨 아래와 오른쪽 맨 위).
  ② 모델이 뒤 조각에 번호를 못 달아도 잡아낸다 — **새 문제는 반드시 번호로
  시작하므로, 번호 없이 글자나 선지로 시작하는 덩어리는 앞 문제의 나머지다.**
  그런 일이 생기는 자리는 **단이 바뀐 첫 덩어리**뿐이라(한 단 안에서는 문제가
  끊기지 않는다) 거기서만 앞 문제에 붙인다.

  **다만 이 규칙은 모델이 번호를 실제로 적어 줄 때만 뜻이 있다.** 아무 조각에도
  번호가 없으면 "번호가 없다"는 사실이 아무것도 알려주지 않는데, 그때도 규칙을
  적용하면 **오른쪽 단의 첫 문제가 통째로 앞 문제에 흡수돼 사라진다**(실제로
  그랬다). 자리만으로는 구분할 수 없다 — "단을 넘어온 뒷부분"과 "오른쪽 단의
  첫 문제"는 둘 다 왼쪽이 바닥까지, 오른쪽이 꼭대기부터다. 그래서 **조각의
  절반 이상에 번호가 있을 때만** 이 규칙을 켠다.

  **잘못 묶으면 문제가 사라지고, 못 묶으면 조각 두 개로 남을 뿐이다.** 그래서
  기준은 전부 안 묶는 쪽으로 기울여 잡았다 — 같은 번호라도 같은 단에서 멀리
  떨어져 있으면(`ADJACENT` 6% 초과) 서로 다른 문제로 본다. 모델이 서로 다른
  문제에 같은 번호를 붙이는 일이 있는데, 그대로 합치면 사이에 있던 문제가
  통째로 사라진다.
  **이어 붙이기는 단을 넘어간 경우에만 쓴다.** 같은 단에 있는 조각은
  `mergeWithinColumn`이 **둘을 아우르는 네모 하나로** 만든다 — 같은 단인데
  이어 붙이면 조각마다 폭을 다시 맞추고 사이에 띠를 넣는 바람에 원래 한
  덩어리였던 것이 잘렸다 붙인 티가 난다. 모델이 한 문제를 발문·자료·선지로
  나눠 주는 일이 잦아서 실제로 그렇게 보였다(프롬프트에도 나누지 말라고
  적었지만, 나눠 오더라도 우리 쪽에서 도로 합친다).
  `stitchVertically`가 읽는 차례대로 세로로 쌓는다. 폭은 **가장 넓은 조각에
  맞춘다** — 좁은 것을 늘리는 쪽이다(넓은 것을 줄이면 글자가 작아져 읽기
  나빠진다). 사이는 아주 좁게 둔다 — 원래 한 문제였으므로 떨어져 보이면 안 된다.
- **영역은 딱 맞게 자른다.** 프롬프트에서 "내용 바깥 경계에 바짝 붙이고 빈
  여백을 넣지 말라"고 못박고, 우리가 더 주는 여유(`PAD`)도 0.4%뿐이다. 넓게
  잡으면 문제 사이의 빈 줄까지 딸려 들어와 문제지에 앉혔을 때 헐렁해진다.
- **찾는 것도 자르는 것도 원본 사진에서 한다.** 화면에 띄우는 축소본
  (`fileToDataUrl`, 긴 변 1600px)에서 자르면 지면 한 장이 1600px 인데 문제
  하나는 그 4분의 1쯤이라 **폭 450px 짜리 조각**이 나온다. 손으로 한 문제만
  찍었을 때 1200~1600px 이 나가던 자리라 3배 넘게 흐려지고, 모델이 본문을 못
  읽어 생성이 실패한다. 영역을 **찾을 때도** 마찬가지다 — 지면에 문제가 열 몇
  개 있으면 문제 하나가 화면의 몇 %밖에 안 돼서, 작게 보내면 경계를 대충 잡는다.
  원본은 data URL 로 만들지 않고 `URL.createObjectURL` 로 읽는다(카메라 사진을
  통째로 문자열로 바꾸면 수십 MB가 되어 일부 브라우저에서 터진다).
- **다만 올려 보내는 크기에는 상한이 있다**(Vercel 요청 본문 4.5MB). 넘으면
  요청 자체가 실패하므로 `detectImage`가 긴 변 3000px 부터 시작해 **실제 길이를
  보고 들어갈 때까지 한 단씩 낮춘다**(3000 → 2400 → 2000 → 1600 → 1200).
  18MB 짜리 사진으로 확인 — 3.03MB 로 맞춰 보낸다.
- 실제 브라우저로 확인했다 — 4문제 지면 → 4조각, 4행 저장, 큐에 4건이
  `mode: "problem"` 으로 들어가 하나씩 처리된다. 단을 넘어 이어진 문제가 든
  지면에서는 3문제로 묶이고 그중 하나가 두 조각을 이어 붙인 세로로 긴 한 장이
  된다(화면에도 "2조각 합침"이라고 표시된다).

### 문제를 통째로 다시 그리기 (탐구용)

크롭 화면에 **"통째로 AI로 다시 그리기"**가 있다. Mathpix를 아예 부르지 않고
문제 한 장을 그대로 이미지 생성 모델에 보내 깨끗한 문제 이미지를 받는다.

**왜 넣었나:** 처음에 사용자가 물었을 때 나는 반대했다 — 이미지 생성 모델은
글자를 틀리고, 틀려도 고칠 방법이 없고, 글자 크기·본문 수정·PDF 다문제 배치를
전부 잃기 때문이다. 그때는 "지금 구조 유지"로 정했는데, 사용자가 **실제로 써보고**
탐구는 이쪽이 낫다고 판단해서 넣었다. 표·지도·그림이 뒤섞인 탐구 문제는 글자로
옮겨 재구성하는 것보다 원본에 가깝게 나온다는 것이다. 기본은 여전히 인식이다.

- 프롬프트가 따로다(`WHOLE_PROBLEM_PROMPT`). 그림용과 요구가 다르다 — 그림은
  모양이 맞아야 하지만 문제 전체는 **글자가 한 자도 틀리면 안 된다**
  ("옳은/옳지 않은" 하나로 답이 뒤집힌다). 그래서 "그대로 옮겨 적기"를 반복한다.
- 입력 해상도가 다르다. 그림 하나는 긴 변 768px(`MODEL_INPUT_DIM`)이면 되지만
  문제 전체는 본문·선지까지 살아야 해서 더 크게 보낸다. **긴 변이 아니라 폭을
  기준으로 맞춘다**(`prepareProblemForModel`, 폭 1536 · 높이 3000). 글자 크기는
  폭으로 정해지므로, 세로로 긴 문제를 긴 변 기준으로 줄이면 폭이 800px 아래로
  떨어져 본문이 뭉개진다 — 단을 넘어 이어 붙인 문제가 특히 그렇다.
- 결과는 **"사용자가 붙인 그림"과 똑같은 취급**을 받는다(`initialFigures`).
  덕분에 저장·수정·PDF가 전부 기존 길을 그대로 탄다 — 그림이 `box_range`에
  저장되므로 나중에 수정 화면에서 다시 열어도 살아 있다.
- 본문 텍스트가 비어 있으므로(`isImageOnly`) 본문 수정·조건 박스·글자 크기
  패널을 감춘다(인식 화면과 수정 화면 양쪽). 다룰 대상이 없는데 열어두면
  사용자가 고칠 수 있다고 오해한다.
- 토큰은 그림 생성과 같은 `FIGURE_TOKEN_COST`(기본 50)를 쓴다. Mathpix를 안
  부르므로 인식 토큰(1)은 들지 않는다.
- **기다리지 않는다.** 생성이 1분쯤 걸리는데 화면을 붙잡아 두면 그동안 아무것도
  못 한다. 그래서 오려낸 원본을 곧바로 카드에 붙여 결과 화면으로 넘기고, 실제
  생성은 그림과 **같은 큐**(`FigureJobsProvider`, `mode: "problem"`)에 맡긴다.
  사용자는 그 사이 정답을 적어 두거나 다음 사진으로 넘어갈 수 있고, 완성되면
  화면이 열려 있으면 미리보기가 갈아끼워지고 닫혔으면 저장본이 갱신된다.
  큐에 넣는 건 `ResultStage`가 한다 — 문제 키(`problemKey`)가 거기서 만들어지기
  때문이다.
- **`enqueue`는 같은 id를 두 번 받지 않는다.** 작업 하나가 곧 유료 API 호출
  한 번이라 중복이 곧 두 배 요금이다. 실제로 개발 모드 StrictMode가 이펙트를
  두 번 실행해 같은 작업이 두 개 쌓였다(패널에 "2개"로 드러났다). 넣는 쪽을
  믿지 말고 큐에서 막는다.

- **글자는 Mathpix 가 읽은 것을 함께 준다.** 이미지 생성 모델은 글자를 자주
  틀리는데, Mathpix 는 반대로 글자를 읽는 일에 맞춰져 있다. 문제 전체를 그릴
  때만 같은 크롭을 Mathpix 에 한 번 보내 본문을 읽고, 그 글을 프롬프트에
  덧붙여 **베껴 쓰게** 한다(`withReference`). 둘의 잘하는 것을 겹쳐 쓰는 셈이다.

  참고 글도 틀릴 수 있으므로 **어긋나면 사진이 우선**이라고 못박는다(안 그러면
  OCR 오류가 그대로 인쇄된다). 표·그림의 배치처럼 참고에 없는 것도 사진을
  따르게 하고, LaTeX 표기는 글자 그대로가 아니라 수식으로 그리게 한다.

  **실패하면 그냥 진행한다.** 참고가 없으면 예전과 똑같이 동작할 뿐이라, 이것
  때문에 그림 생성을 막을 이유가 없다. mock 응답(키 미설정)은 가짜 글자라
  참고로 쓰지 않는다. 인식 토큰이 하나 더 들지만 문제 전체를 다시 그리는
  값(50)에 비하면 작고, 글자가 틀리는 쪽이 훨씬 비싸다.

  **됐는지 안 됐는지가 눈에 보인다.** 참고가 붙었는지는 결과물의 글자 정확도를
  좌우하는데, 조용히 실패하면 사용자는 "왜 글자가 틀렸지"만 보고 이유를 알 수
  없다. 작업마다 `FigureJob.ocr`(`reading` / `ok` / `none`)을 두고
  `FigureJobsPanel`이 그대로 보여 준다 — 읽는 중 / 참고 ✓ (읽어 낸 앞 60자를
  함께) / 참고 없음(그림만 보고 그림). 실제 브라우저로 세 상태를 확인했다.
  **캐시에 걸린 작업에는 아무 줄도 안 나온다** — 그때는 Mathpix 를 부르지
  않으니 그게 맞다.

**두 프롬프트에 공통으로 넣은 것**(그림용·문제 전체용 둘 다):

- **필기 흔적은 지우되 인쇄된 강조는 지킨다.** 이게 헷갈리기 쉽다 — 손으로 그은
  밑줄·동그라미·채점 표시는 지워야 하지만, **인쇄된 밑줄과 굵은 글씨는 문제의
  일부다**(밑줄 친 낱말을 묻는 문제가 흔하다). 여기를 뭉뚱그리면 모델이 문제의
  밑줄까지 지운다. 지시는 `ERASE_HANDWRITING` 한 곳에 모아 두 프롬프트가
  똑같이 쓴다.

  **색으로 구분하라고 하면 안 된다.** 처음에는 "삐뚤고 색이 다른 것 vs 반듯하고
  검정인 것"으로 적었는데, 그러면 모델이 **검은 볼펜으로 쓴 손글씨를 인쇄물로
  보고 그대로 남긴다**. 지금은 획 굵기가 들쭉날쭉한지·서체가 인쇄 글자와
  다른지·칸 밖으로 삐져나갔는지로 가르게 하고, 색은 보조 단서로만 둔다.
  지우고 끝내지 말고 **가려져 있던 인쇄 내용을 되살려 그리라**는 것도 함께
  적었다 — 안 그러면 지운 자리가 빈칸으로 남는다.

  **그리고 이 지시는 프롬프트 맨 앞에 둔다.** 한때 프롬프트 중간(색·명암 앞)에
  두고 항목만 늘렸더니 오히려 **전보다 덜 지웠다**. 길게 적을수록 묻힌다 —
  짧고 앞에 두는 편이 낫다.
- **원문자(㉠ ① ⓐ)는 "만드는 법"으로 알려 준다.** 이미지 생성 모델이 유독 약한
  자리다 — 사람 눈에는 동그라미 + 글자지만 모델에게는 **글자 하나**(㉠ 은 한
  코드포인트다)라, 통째로 기억해 그리려다 안쪽 자음이 딴것으로 바뀐다. 그래서
  "원을 긋고 그 안에 글자를 넣어라" + 순서표(㉠=ㄱ, ①=1)를 준다(`CIRCLED_CHARS`).
  `밑줄 친 ㉠에 대한 설명으로…` 처럼 원문자가 곧 지시 대상이라 한 자만 틀려도
  문제가 성립하지 않는다.

  **Mathpix 참고 글에 실제로 나온 것만 따로 짚어 준다**(`circledNote`).
  `insideCircle()` 이 원문자를 안에 든 글자로 풀어 "㉠(동그라미 안에 ㄱ)" 표를
  만든다. NFKD 로는 안 된다 — 원이 사라진 글자만 남을 뿐 "동그라미 안에 무엇"
  이라는 문장이 되지 않는다. 자음 표(U+3260~)를 직접 들고 있는 이유는 ㄱ~ㅎ
  구간(U+3131~)이 쌍자음·겹받침 때문에 이어져 있지 않아서다.
  **나온 차례가 아니라 원문자 차례로 늘어놓는다** — 본문이 `㉠~㉢` 처럼 범위로
  먼저 나오면 나온 차례는 ㉠ ㉢ ㉡ 이 되는데, 바로 아래에서 "순서를 뒤바꾸지
  말라"고 해 놓고 우리가 뒤바꾼 목록을 주는 꼴이 된다.
  하나도 없으면 한 줄도 넣지 않는다(길게 적을수록 묻힌다).
- **색은 명암으로도 구분되게.** 결과물은 대개 흑백으로 인쇄된다. 지도의 지역이나
  그래프 계열이 색으로만 갈려 있으면 인쇄하는 순간 같은 회색이 된다. 그래서
  "색은 유지하되 밝기 차이나 빗금·점무늬를 함께" 넣게 했고, 범례가 있으면 범례와
  본문의 표시를 같은 진하기로 맞추게 했다.
  ("사진처럼 명암 넣지 말라"는 기존 지시와 충돌해 보이므로, 그 줄에 이건 예외라고
  명시해뒀다 — 안 그러면 모델이 둘 중 하나를 버린다.)
- **작은 표시를 빼지 않게 나열했다**: 눈금·눈금 숫자·축 이름·단위·범례·화살표
  머리·점선/실선 구분·각도 호·직각 표시·첨자·소수점. 두루뭉술하게 "세부까지"라고만
  적으면 모델이 알아서 생략한다.
- 문제 전체 쪽에도 **교점** 지시를 넣었다(탐구에도 그래프가 나온다).

### 사회탐구·과학탐구 자료 (수학 도형과 완전히 별개)

수학 도형은 Gemini, **사과탐 자료는 OpenAI**(`OPENAI_API_KEY`)를 쓴다. 두
경로는 코드가 겹치지 않는다 — 수학 쪽은 이미 안정적으로 돌아가고 있어서
건드리지 않는 게 원칙이다. 결과가 둘 다 SVG 문자열이라 문제 카드에 붙고
저장되는 뒷단(`manualDiagramSvgs`)만 공유한다.

- `src/lib/figureImageGen.ts` — OpenAI **이미지 편집** API(`/v1/images/edits`)
  호출. multipart/form-data이고 Content-Type을 직접 지정하면 안 된다(fetch가
  경계값까지 붙여준다).
- `src/app/api/figure/route.ts` — 인증·과금·모델 폴백. `maxDuration = 60`.
- `src/app/api/figure/models/route.ts` — **프로브 엔드포인트**(로그인 필요).
- `src/components/FigurePanel.tsx` — UI 전체.

**SVG가 아니라 이미지 생성을 쓴다.** 처음에는 비전 모델에게 SVG 마크업을
뱉게 했는데(`figureVector.ts`, 지금은 삭제) 사용자가 실제로 써보고 품질이
부족하다고 판단했다. 차이가 근본적이다 — SVG 방식은 모델이 그림을 "코드로
설명"해야 해서 좌표를 하나하나 지어내야 하지만, 이미지 편집은 원본 이미지를
그대로 입력으로 받아 정리한 그림을 낸다. 되돌리려면 git 이력에서
`figureVector.ts`를 찾을 것.

대신 결과가 래스터라 확대하면 흐려지고, **이미지 생성 모델은 한글 라벨을
잘못 쓰는 경우가 있다.** 글자가 중요한 자료는 "원본 그대로 붙이기"가 여전히
가장 정확하다. 완성된 그림은 원본 크롭과 똑같이 `rasterToSvg`로 감싸서
크기·위치 조절과 PNG 캡처가 두 경로에서 같은 방식으로 돌게 한다.

모델은 **`gpt-image-2` 하나뿐이다.** 2026-08 프로브에서 `gpt-image-1.5` /
`gpt-image-1` / `gpt-image-1-mini`도 이 계정에 존재함을 확인했지만 후보에서 뺐다.

**폴백 캐스케이드를 걷어낸 이유 (다시 넣으려는 다음 사람에게):** 원래는 후보를
여러 개 늘어놓고 앞의 것이 404/403/429면 다음으로 내려가게 했다. 모델 이름을
틀려도 기능이 죽지 않게 하려던 것이다. 그런데 그 대가로 **사용자가 고른 적도
없는 모델(gpt-4o-mini, gpt-4.1, gpt-5 등)에 요금이 나갔다.** SVG 시절에는
폴백 한 번이 출력 8000 토큰짜리 생성이라 특히 비쌌다. 실패했을 때 조용히 다른
모델로 갈아타는 것보다 실패했다고 알리고 멈추는 편이 낫다. 폴백이 필요하면
`OPENAI_FIGURE_IMAGE_MODELS`에 명시할 것.

방어선도 두 겹 두었다 — `figureImageModelIds()`가 `gpt-image`로 시작하지 않는
이름을 걸러내고, `generateFigureImage()`가 호출 직전에 한 번 더 확인한다.
환경변수 오타나 실수로 채팅 모델 이름이 들어가도 그쪽으로는 요청이 나가지 않는다.

`/api/figure/models` 프로브도 **지금 설정된 후보만** 찔러본다(기본값이면 1회).
예전에는 채팅 모델 6개를 매번 실제로 호출했고, 그것도 의도치 않은 사용량의
일부였다. 목록 조회(`/v1/models`)는 무료다.

**60초로는 모자란다 — `maxDuration = 300`.** 운영 로그에서 문제 한 개 전체를
그리는 요청이 `Vercel Runtime Timeout Error: Task timed out after 60 seconds`
로 죽는 게 확인됐다(같은 시간대에 성공한 것도 있어 60초에 걸쳐 있었다).
품질을 낮춰 시간을 줄이는 건 답이 아니다.

**시간이 다 되면 우리가 먼저 끊는다**(`DEADLINE_MS = maxDuration - 15초`).
Vercel 이 함수를 죽이면 **환불 코드가 아예 돌지 못해서** 토큰(50)만 나가고
아무것도 안 남는다 — 실제로 그렇게 잃고 있었다. `AbortController` 를 넘겨
우리가 끊으면 그 뒤를 이어서 환불하고 읽을 수 있는 오류를 돌려줄 수 있다.
타이머는 `finally` 에서 꼭 지운다(인스턴스가 재사용되면 엉뚱한 요청을 끊는다).

**`gpt-image-2` 는 `input_fidelity` 를 받지 않는다.** 그래서 `PARAM_VARIANTS`
의 첫 조합은 그걸 뺀 `{ size: "auto" }` 다. 예전에는 `input_fidelity: "high"`
를 앞에 두고 거부당하면 다음으로 갔는데, **거부당하는 요청도 이미지를 통째로
올린 뒤에야 거부당한다** — 문제 한 장이 3MB쯤이라 매 요청마다 3MB를 헛되이
올리고 있었다. 운영 로그에서 성공한 요청까지 전부 이 400을 한 번씩 맞고 있는
게 보였다. 한 번 통한 조합을 기억하는 `workingVariant` 로는 못 막는다 —
모듈 수준 Map 이라 **서버리스 인스턴스마다 새로 빈다.**

**비용은 거의 전부 출력 그림 토큰이다.** 요금은 입력 글자 $5/1M · 입력 그림
$8/1M · **출력 그림 $30/1M**(캐시 읽기는 글자 $1.25 · 그림 $2)이고, 요청당
비용의 **83%가 출력 토큰**이다(입력 그림 8.6%, 입력 글자 8.0%). 입력 해상도나
프롬프트 길이를 줄여봐야 나머지 17%를 건드리는 것이라 효과가 작다 — 실제로
Mathpix 참고를 붙여 입력 글자가 550 토큰 늘었을 때 요청당 $0.00275 였다.

**단가는 공표된 요금표를 그대로 쓴다. 청구액에서 역산하지 말 것.** 한때
역산해서 썼다가 틀렸다 — 입력 글자를 $2.5(실제의 절반), 입력 그림을 $10(실제보다
비싸게)으로 잡아 하루 최대 $0.077 어긋났다. 합계만 맞추는 단가 조합은 여럿이라
역산으로는 항목을 가려낼 수 없다. 공식 값으로 계산하면 **실제 청구액과 센트
단위까지 맞는다**(다른 모델이 안 섞인 6일 전부 일치 — 예: 8/18 추정 $4.071712 /
청구 $4.07171).

**그래서 출력 캔버스를 입력 비율에 맞춘다**(`pickOutputSize`). `size: "auto"`
로 두면 모델이 제 비율로 그리고 둘레에 흰 여백을 붙여 주는데, 우리는 그 여백을
받자마자 `trimBlankBorder()` 로 잘라 버린다 — **돈 내고 받은 픽셀을 그대로
버리는 것이다.** 비율을 맞추면 같은 토큰으로 내용이 캔버스를 꽉 차므로 실효
해상도는 오히려 올라간다. 크기를 재는 일은 브라우저가 한다(`imageSizeOf`) —
서버에는 이미지를 여는 수단이 없어서 요청 본문에 `width`/`height` 로 실어
보낸다. 없으면 예전처럼 auto 다. `OPENAI_IMAGE_SIZE` 로 강제할 수도 있다.

**요청마다 usage 를 찍는다.** 청구서가 하루 단위로만 나와서 무엇이 비용을
끌어올리는지 끝내 단정하지 못한 적이 있다(출력 토큰이 왜 뛰었는지). 이제
`[figureImageGen] usage ... size=... 입력=WxH in=... out=... est=$...` 가
남으므로 크기·모드별로 바로 견줄 수 있다(캐시가 걸린 요청에는 `cached=N` 이
붙는다).

**캐시는 localStorage 다**(예전엔 sessionStorage). 탭을 닫으면 캐시가 사라졌는데
**재시도가 곧 재과금인 상황이 바로 그 자리**였다 — 타임아웃 뒤 다시 들어와
누르면 같은 크롭에 유료 호출이 한 번 더 나갔다.

**금액은 원으로 보여 준다**(`USD_TO_KRW`, 기본 1400). 요금은 달러로 나가지만
읽는 사람은 원이 감이 온다. 좁은 머리줄은 원만(`약 223원`), 작업마다는 둘 다
(`약 $0.067 (93원)`) 적는다. 환율 계산도 **서버가 해서 내려보낸다**(`estKrw`)
— 화면에서 따로 곱하면 로그와 어긋난다. 환율은 움직이므로 환경변수로 두어
재배포 없이 고칠 수 있다.

**진행 패널에 "생성 N회 · 약 223원"이 뜬다.** 요청 수가 문제 수보다 훨씬
많아지는 것이 비용의 주범인데(20문제에 94회 나간 적이 있다) 화면에 아무 단서가
없었다. 금액도 Vercel 로그나 OpenAI 대시보드를 열어야만 보였다. 지금은 작업이
끝나면 그 자리에서 보이고, 작업마다 제 비용도 붙어 **어느 문제가 비쌌는지**
알 수 있다. 캐시에 걸린 것은 세지도 더하지도 않는다 — 그건 돈이 안 나갔다.

**로그와 화면이 같은 함수(`readUsage`)를 쓴다.** 양쪽에서 따로 계산하면
어긋나는데, 그러면 어느 쪽이 맞는지 알 방법이 없다. 서버가 계산해
응답(`usage.estUsd`)으로 내려보내고 화면은 받아서 더하기만 한다.
**금액 자체는 공표된 요금으로 계산한 값이라 정확하고, 어림인 것은 원화 환산뿐이다**
— 패널에도 "1달러=N원 기준", "최종 청구액은 OpenAI 대시보드를 보세요"라고 적어 둔다
(환율은 서버가 `krwRate` 로 함께 내려보내 화면에 숫자를 하드코딩하지 않는다). 어디서 무엇을 볼 수 있는지는 이렇다:
OpenAI 대시보드 = 실제 청구액(하루 단위·하루쯤 지연) /
Vercel 로그 = 요청별 토큰과 추정액(즉시) / 앱 패널 = 이번 세션 합계(즉시).

**`quality`는 보내지 않는다.** 한때 시간을 아끼려고 `medium`을 넣었는데,
자료는 글자와 가는 선이 살아야 쓸모가 있어서 품질을 낮추면 안 된다.
60초 안에 끝나는 게 정상이고, 안 끝나면 품질이 아니라 자료 영역을 좁혀야 한다.
`input_fidelity: "high"`(원본을 얼마나 따라갈지)는 유지하되, 안 받는 모델이면
400이 나므로 `PARAM_VARIANTS`의 다음 조합으로 내려간다.

**프로브 이미지를 손으로 적지 말 것.** `models/route.ts`의 `TINY_PNG_BASE64`를
처음에 기억으로 적었다가 IDAT 청크 CRC가 깨진 잘린 파일을 넣었고, 그 결과
멀쩡한 네 모델이 전부 "Invalid image file or mode"로 400을 냈다. 진단 도구가
정상인 것을 불량이라고 보고한 셈이다. 바꿔야 하면 실제로 파일을 만들어
CRC까지 검사한 뒤 넣을 것.

**응답 필드를 바꿀 때 조심할 것.** 카드는 HTML 문자열을 이어붙여 만들기
때문에 마크업 자리에 `undefined`가 하나 섞이면 "undefined"라는 글자가 그대로
문제에 인쇄된다. 응답 필드를 `svg`에서 `image`로 바꿨을 때 예전 화면이 캐시된
상태에서 실제로 그 일이 났다. 지금은 두 군데서 막는다 — `FigurePanel`이
응답의 `image`가 `data:image/`로 시작하는 문자열인지 확인하고, `ResultStage`의
`figures`가 빈 마크업을 걸러낸다.

**비용 설계 (이게 이 기능의 핵심이다)**

1. **자료를 오려낸 뒤 바로 보내지 않는다.** "원본 그대로 붙이기(무료)"와
   "AI로 다시 그리기(사진인식권 5장)" 중 사용자가 고른다. 무료 쪽이 기본이자
   앞에 있다 — 사진·현미경 사진·지도는 다시 그리면 정보가 사라지므로 원본이
   정답이고, 사과탐에는 그런 자료가 아주 많다.
2. **입력 축소** — 긴 변 768px(`MODEL_INPUT_DIM`)로 줄여 보낸다.
3. **캐시** — 같은 자료는 한 번만 보낸다(`figureCache.ts`, sessionStorage).
   사과탐은 자료 하나에 문항이 2~3개 딸린 세트가 흔해서 실제로 자주 걸린다.
4. **출력 상한** — `MAX_OUTPUT_TOKENS = 8000`. 비용의 대부분이 출력 토큰이다.

**자동 판별기는 일부러 안 넣었다.** "사진형이면 호출을 막자"고 픽셀 통계로
선화/사진을 가르려 했는데 두 방법 다 실패했다: ① 국소 평탄도 → 부드러운
그라디언트 사진이 1.00으로 선화(0.90~0.95)보다 높게 나온다(그라디언트는
국소적으로 가장 평탄하다). ② 상위 N개 색 점유율 → "색칠된 지층 단면도를
어두운 조명에서 찍은 사진"이 0.59로 사진 범위(≤0.63)에 파묻힌다. 어느
임계값을 잡아도 정상 자료를 막거나 사진을 통과시킨다. 다시 시도하려면
`figureImage.ts` 주석의 수치부터 읽을 것.

**모델 이름은 여기서도 추측하지 말 것.** `figureImageGen.ts`의
`DEFAULT_IMAGE_MODEL_IDS`는 계정의 `/v1/models` 목록에서 가져왔을 뿐
**실제 호출로 검증되지 않았다.** `/api/figure/models`가 후보마다 8×8 PNG로
진짜 편집 요청을 보내보고 결과를 알려준다. 로그인 후
`/api/figure/models`를 열면 ① `/v1/models` 목록과 ② 후보마다 1×1 PNG를
**실제로 보내본** 프로브 결과가 같이 나온다. `usable`에 나온 이름을
`OPENAI_FIGURE_IMAGE_MODELS` 환경변수에 쉼표로 넣으면 **재배포 없이** 적용된다.
호출 중 404/403/429가 나면 그 자리에서 다음 후보로 자동으로 내려간다.

과금은 **새 마이그레이션 없이** 기존 `consume_recognition_credit(p_amount)`를
쓴다(사진인식권). 차감은 요청당 딱 한 번이고 — 모델을 갈아타는 건 우리
사정이지 사용자가 더 낼 이유가 아니다 — 실패하면 `refund_recognition_credit`로
되돌린다. 장수는 `FIGURE_CREDIT_COST`(기본 5), 화면은 `/api/figure/config`로
그 값을 읽어간다. **JS에 하드코딩하지 말 것.**

### 표 처리 (Mathpix가 보내는 형태가 아주 여러 가지다)

Mathpix는 표를 여러 형태로 보낸다. `protectTables()`가 문단/줄 분리 **전에**
전부 HTML `<table class="mmd-table">`로 바꿔 토큰으로 감춘다.

1. **마크다운 표** — Mathpix mmd의 기본 형태다. 이게 오래 처리되지 않아서
   `| z | P |` 같은 줄이 글자 그대로 찍혔다. 머리글 다음 줄이 구분선
   (`| :---: | :---: |`)인지로 표를 알아본다. 셀을 쪼갤 때 `$...$` 안의 `|`는
   구분자로 보지 않는다(절댓값 기호).
   변형 둘을 더 받는다 — **바깥 파이프가 없는 것**(`구분 | 값`)은 구분선
   (`:---: | :---:`)으로 알아보고, **구분선이 아예 없는 것**은 바깥 파이프가
   있고 **칸 수가 같은 줄이 둘 이상** 이어질 때만 표로 본다. 그냥 "`|`가 있는
   줄"로 잡으면 `$|x|=3$`이나 `\{x \mid x>0\}` 같은 문장이 표로 둔갑한다
   (오탐 시험 9가지로 막아뒀다).
2. **`$$`/`$` 안의 tabular** — `TABULAR_PATTERN`이 앞뒤 델리미터까지 같이
   먹는다. 안 그러면 `$\x00TABLE0\x00$`가 남아 KaTeX가 NUL을 파싱하다 실패하고,
   **실패한 원문("\displaystyle ...")이 빨간 글씨로 화면에 그대로 찍힌다.**
   사용자가 "무슨 display 어쩌구 하면서 깨진다"고 한 게 이거였다.
   `$$\displaystyle\begin{tabular}`처럼 스타일 명령이 끼는 것도 받는다.
3. **맨몸 tabular** — 원래부터 되던 것.
4. **tabularx / tabular\*** — 열 지정 앞에 폭 인자가 **하나 더** 붙는다
   (`\begin{tabularx}{\textwidth}{|X|X|}`). 인자 개수가 달라서 환경별로 나눠 적었다.

**델리미터는 여는 것과 닫는 것을 짝으로 적어야 한다.** 닫는 쪽만 선택적으로
(`\s*(?:\$\$|\$)?`) 두면 표 뒤의 빈 줄을 삼킨 뒤 **다음 문장의 여는 `$`**를
닫는 델리미터로 집어먹는다 — 실제로 "$P(0<Z<1)$의 값은?"이 통째로 표 안으로
빨려 들어갔다. 그래서 `$$…$$` / `$…$` / 맨몸 세 가지를 따로 늘어놓는다.

**병합 셀**(`\multicolumn` / `\multirow`)은 사회탐구 표에 아주 흔하다.
- **내용은 마지막 인자다.** `\multicolumn{2}{|c|}{인구 구성}` — 가운데(정렬)를
  내용으로 읽으면 칸에 `|c|`가 찍힌다. 실제로 그렇게 짰다가 놓쳤는데,
  "`multicolumn`이라는 글자가 안 남았나"만 검사한 테스트가 통과해 버려서
  **브라우저로 그려보고서야** 발견했다. 표 관련 테스트는 칸 내용까지 볼 것.
- `\multirow`는 다음 행에 **빈 자리표시 칸**을 남긴다(`& 면적`의 앞 빈 칸).
  LaTeX는 그게 있어야 하지만 HTML `rowspan`은 없어야 한다 — 안 버리면 칸이
  하나씩 밀려 표가 어긋난다. 열마다 남은 행 수를 세어(`covered`) 버린다.
- `\cline{1-2}` / `\toprule` 등은 가로줄 지시라 `\hline`처럼 지운다.

`TABULAR_PATTERN`은 **열 지정 앞의 배치 옵션**(`\begin{tabular}[t]{|l|l|}`,
`[b]`)과 **열 지정 안의 중괄호**(`@{}ll@{}`)를 둘 다 받아야 한다. 처음 패턴은
`\{[^}]*\}` 하나뿐이라 `[t]`가 붙은 표에 안 걸렸고, 그러면 표가 통째로 KaTeX로
넘어가 2번과 똑같은 증상이 난다 — 사용자가 "여전히 이런 식으로 뭔가 이상함,
계속 렌더링이 안 됨"이라고 한 사회탐구 문제가 이 경우였다. 옵션 없음 / `[t]` /
`[b]` + 공백 / `@{}` 중첩 / `$$` 안 + `[t]`, 다섯 형태를 테스트로 확인했다.

**앞뒤 공백은 `$$`에 붙은 것만 먹는다.** 예전에는 그냥 `\s*`라 표 앞뒤의 빈
줄까지 삼켰고, 그러면 표 토큰이 앞 문장과 같은 줄에 붙어 표가 **문단 안에**
갇혔다(마크다운 표는 제 줄에 남아 별도 블록이 되는데 tabular만 그러지 못해,
같은 표인데 형식에 따라 다르게 그려졌다). 표를 손으로 끌어 옮기려면 표가 제
블록이어야 해서 고쳤다. 부작용 하나: 이 텍스트의 `lines` 줄 번호가 달라진다 —
tabular 표가 있고 **박스 범위를 손으로 지정해 저장한** 옛 문제는 박스가 다른
줄에 걸릴 수 있다(자동 감지로 저장된 것은 영향 없음).

(`$$\begin{array}...$$`는 KaTeX가 직접 그리므로 건드리지 않는다.)

### 표도 그림처럼 끌어 옮긴다 · 가로로 나란히 놓기

표는 이제 문단이 아니라 **옮길 수 있는 물건**이다. `RenderedBlock`에
`{ kind: "table", id, html }`가 생겼고, `cardHtml.ts`가 표를 그림과 똑같이
다룬다(`CardFigure`가 둘을 함께 받는다).

- `buildAnchors`는 **표 블록을 건너뛴다.** 표가 자기 자리를 만들면 "자기 뒤에
  자기를 놓는" 자리가 생기고, 화면의 DOM 순회(`anchorPoints`)와도 어긋난다.
- `collectTables()`가 표마다 `defaultPosition`(원문에서 있던 자리)을 준다.
  손대지 않으면 예전과 **글자 하나까지 같은** 출력이 나온다(테스트로 고정).
- `cardHtmlFromSpec`은 spec에 표가 없으면(예전에 저장된 형식) 원래 자리에
  끼워 넣는다. 안 그러면 화면이 닫힌 뒤 다시 그릴 때 표가 통째로 사라진다.
- 표 기본값은 `DEFAULT_TABLE_LAYOUT`(폭 100%, 위 여백 0)이다. `.mmd-table`
  자신의 `1.1em` 여백을 그대로 살려 간격이 예전과 같게 했다. 껍데기 안에서는
  표가 늘 마지막 자식이라 `.mmd-table:last-child`가 항상 걸리므로,
  `.problem-figure--table` 규칙은 **globals.css에서 그 규칙보다 뒤에** 있어야
  한다(특정도가 같아서 앞에 두면 아래 여백이 통째로 사라진다).

**가로로 나란히**는 같은 자리에 놓인 것들에 `row`를 켜면 `.problem-figure-row`
(flex)로 묶인다. 혼자면 아무 일도 없다. 나란히일 때는
- 폭을 `width` 대신 `flex:0 1 N%`로 준다(둘을 합쳐 100%를 넘어도 알아서 줄어
  카드 밖으로 안 나간다),
- 위 여백은 각자가 아니라 **줄 전체**가 갖는다(`rowStyleCss`, 가장 큰 값).
  각자 두면 그림 기본값 16px과 표 0px이 어긋나 나란히 놓은 티가 안 난다,
- **가로 순서는 `offsetX` 값으로 정한다** — 미리보기에서 옆으로 끌면 그대로
  자리가 바뀐다. 그래서 나란히일 때 `offsetX`는 여백으로 쓰지 않는다(쓰면
  순서가 바뀌면서 같이 밀려 이상해진다). 조절 패널의 "좌우"도 이때는
  "가로 순서"로 이름이 바뀐다.

### 그림은 저장된다 (안 그러면 수정할 때 사라졌다)

예전에는 저장되는 게 **합쳐진 PNG 한 장과 본문**뿐이었다. 그런데 "문제 내용
수정" 화면은 본문으로 카드를 **다시 그린다.** 그림 정보가 없으니 그림 없는
카드가 만들어졌고, 저장하면 그대로 덮어써져 **원래 붙어 있던 그림이 사라졌다.**

`src/lib/storedFigures.ts`가 `problems.box_range` jsonb에 그림을 함께 담는다
(마이그레이션 없이 — `fontPt`를 거기 얹은 것과 같은 이유, `fontSize.ts` 참고).
저장 형태는 `{ ranges, fontPt, figures }`.

- **표는 마크업을 저장하지 않는다.** 표는 본문에서 다시 만들어지므로
  (`collectTables`) 같이 저장하면 본문을 고쳤을 때 저장본이 낡은 표를 들고
  있게 된다. 표는 사용자가 옮겨둔 자리·크기만 저장한다.
- `readStoredFigures()`가 값의 모양을 확인하고 받는다. 이 문자열은 카드에
  그대로 이어붙여지므로(`dangerouslySetInnerHTML`), `<img>`/`<svg>`로 시작하지
  않거나 `<script`가 섞인 값은 버린다.
- **목록 조회에서는 그림을 빼고 가져온다.** 문제 하나당 수백 KB라 `select("*")`
  로는 실모 하나 여는 데 수 MB를 읽는다. `page.tsx`가 필요한 키만 뽑고
  (`ranges:box_range->ranges` 같은 PostgREST JSON 경로 선택), 그림은 "수정"을
  열 때 그 문제 하나만 따로 가져온다. **옛 형태 키(`start`/`end`/`none`)도 같이
  뽑아야 한다** — 안 그러면 그때 손으로 잡아둔 박스가 자동 감지로 되돌아간다.
  JSON 경로 선택이 실패하면 `select("*")`로 되돌아간다(목록이 통째로 안 나오는
  것보다 낫다).
- **이미 저장된 옛 문제는 소급이 안 된다.** 그림 정보가 남아 있지 않다. 수정
  화면이 그 사실을 알아채고("figures 키가 아예 없음") 경고를 띄운다.

### 수정 화면에서도 그림을 다루게 했다

예전에는 수정 화면에서 그림의 **자리와 크기만** 만질 수 있었다. 잘못 오려냈거나
원본 그대로 붙여 둔 것을 다시 그리고 싶으면 문제를 지우고 처음부터 해야 했다.
지금은 셋을 할 수 있다 — **그림 추가 · 다시 오려내기 · AI로 다시 그리기**
(크기·여백은 원래대로 `DiagramAdjuster`).

- 원본 사진은 저장하지 않으므로(변환 결과만 남긴다) **새 그림은 새로 찍은
  사진에서만** 오려낼 수 있다. 다시 오려내기는 붙어 있는 그림 자체가 재료다
  (`rasterFromSvg`가 마크업에서 원본 픽셀을 되꺼낸다 — `<svg><image href=…>`와
  옛 형태 `<img src=…>` 둘 다).
- **이미 AI로 그린 그림은 다시 그릴 수 없다**(`StoredFigure.ai`). AI 결과를 또
  AI에 넣으면 원본에서 멀어지기만 하고 요금은 두 번 나간다. 다시 오려내면 그
  픽셀은 원본이므로 표시가 떨어지고 다시 그릴 수 있게 된다.
  **옛 데이터에는 이 값이 없다**(구분할 방법도 없다). 그때는 막지 않는다 —
  막으면 원본 크롭인 그림까지 손댈 수 없게 된다.
- 큐는 인식 화면과 **같은 것**을 쓴다(`FigureJobsProvider`). 그래서 수정 창을
  닫아도 작업이 계속 돌고, `putSnapshot`으로 지금 상태를 알려 두므로 창이 닫힌
  뒤 끝난 작업도 저장본에 반영된다.
- `enqueue`는 같은 id를 두 번 받지 않으므로(중복 과금을 막는 자리다) 다시 그릴
  때는 `dismiss`로 옛 작업을 먼저 지운다.

### 카드와 드래그는 DraggableCard 하나만 쓴다

인식 결과 화면과 수정 화면이 **같은 것을 써야 한다.** 자리 계산이 조금만 달라도
그림이 엉뚱한 데 붙는다. 카드 조립은 `cardHtml.ts` 한 곳, 그걸 화면에 얹고 끌게
하는 건 `DraggableCard.tsx` 한 곳이다.

### 드래그는 window에서 받는다 (setPointerCapture 아님)

`setPointerCapture`에 기대면 안 된다. **안내선이 나타나는 것만으로 카드가 한 번
다시 그려져서**, 처음 잡은 DOM 노드가 곧 떨어져 나가고 캡처가 조용히 풀린다
(실제 브라우저로 확인 — pointerdown 직후 본문 자식이 통째로 교체된다. 이건
이번 작업 전부터 그랬다). 그래서 카드 **바깥 여백에서 손을 놓으면 pointerup이
어디에도 닿지 않아 옮긴 게 무시됐다** — "맨 위/맨 아래로 보내기"가 바로 그
자리라 특히 자주 걸렸다.

지금은 pointerdown에서 `window`에 pointermove/up을 걸고, 움직일 때마다
`figureEl(id)`로 지금 화면에 있는 요소를 다시 찾아 스타일을 준다. window에 거는
함수는 **항상 같은 참조**여야 뗄 수 있으므로, 매 렌더 갱신되는 실제 handler를
ref에 담아두고 얇은 래퍼만 등록한다.

### 본문 편집 — 보이는 대로 고치기

LaTeX 원문을 직접 고치는 걸 사람들이 어색해했다. `TextEditTabs`가 두 방식을
제공하고 기본은 **"보이는 대로"**다.

`mathTokens.ts`가 원문을 글자/수식 조각으로 쪼개고(`tokenizeMath`), 편집이
끝나면 다시 이어 붙인다(`joinMathTokens`). **AI는 쓰지 않는다** — 순수 문자열
분해·결합이라 손대지 않은 부분은 한 글자도 바뀌지 않는다(왕복 8건 확인).

`VisualTextEditor`는 글자 조각을 textarea로, 수식은 렌더링된 칩으로 보여준다.
칩을 누르면 그 수식만 따로 고칠 수 있다.

**글씨 분수**(분자·분모가 한글 문장인 분수)는 따로 다룬다. 수능 사과탐·수학에
자주 나오는데 **Mathpix가 이걸 자주 놓친다** — 실제 사례에서 분모를 반쯤 읽다
흘리고 "…X 의 함량(%) 0.5 억 년 전 P 에 포는" 처럼 뭉개서 보냈다. 인식은 우리가
못 고치므로 손으로 만들 수 있게 했다: 글자 조각의 "분수 넣기"가 커서 자리에
분수를 끼우고, 그 칩을 누르면 LaTeX가 아니라 **분자/분모 칸**이 열린다
(`buildTextFraction` / `parseTextFraction`).

**연속 공백도 살린다.** HTML은 공백 여러 칸을 한 칸으로 붙여버려서 본문에서
간격을 벌려놔도 반영되지 않았다. `preserveSpaces()`가 마지막 한 칸만 보통
공백으로 두고 나머지를 NBSP로 바꾼다 — 전부 NBSP로 바꾸면 긴 공백에서 줄이
안 넘어가 카드가 삐져나간다.

**contentEditable 한 덩어리로 만들지 않은 이유**: 브라우저마다 만들어내는
마크업이 달라(줄바꿈을 `<br>`로 넣는 곳, `<div>`로 감싸는 곳) 거기서 원문을
복원하면 반드시 어긋난다. 조각을 따로 두면 무엇이 글자이고 무엇이 수식인지
항상 확실하다.

### 글자 크기 (pt, 문제마다 저장된다)

`src/lib/fontSize.ts`. 인쇄물이라 단위가 pt다(CSS는 96dpi 기준이라 1pt = 4/3 px).
기본 15pt = 20px로, 예전 "보통"과 같은 크기다. 프리셋 세 개 + pt 직접 입력.

**저장 위치가 `problems.box_range`인 이유**: 새 컬럼을 만들려면 마이그레이션이
필요한데, 안 돌린 사람에게는 저장 자체가 실패한다(없는 컬럼에 쓰면 에러).
이미 있는 jsonb에 키를 얹으면 그 위험이 없다. 컬럼 이름이 실제 내용보다
좁지만 안전을 택했다. 저장 형태는 `{ ranges, fontPt }`이고 **`ranges: null`이
"박스는 자동 감지"**를 뜻한다 — 크기만 저장하려고 객체를 써야 해서 필요하다.
옛 값(`{start,end}` / `{none:true}` / `null`)은 그대로 읽힌다.

고쳤던 버그: `ProblemGallery`의 미리보기가 글자 크기를 **24px로 하드코딩**해
둬서, 보통(20px)으로 저장한 문제도 수정만 하면 글씨가 저 혼자 커졌다.

### 오답 목록 — 카드 / 목록 두 가지로 본다

순서를 바꿀 때 카드는 불편하다. 한 화면에 두세 개밖에 안 들어와서 멀리 보내려면
계속 스크롤해야 한다. `ProblemGallery`에 **목록 보기**를 넣었다 — 한 줄에 하나씩
(자리 번호 · 썸네일 · 본문 앞부분 · 정답 · ↑↓ · 수정 · 삭제). 고른 보기는
localStorage에 남는다.

**자리 번호를 직접 적을 수 있다**(`OrderInput`). 화살표만으로는 20번을 1번으로
보내려면 열아홉 번을 눌러야 한다. Enter나 칸을 벗어날 때 반영하고, 반영 뒤에는
칸을 원래 번호로 되돌린다 — 자리가 안 바뀌거나 저장에 실패해 되돌아갔을 때
적어 둔 값이 남아 화면과 어긋나던 것을 막는다(실제로 그랬다).

**`sort_order` 값은 새로 만들지 않는다.** 자리마다 붙어 있던 값을 그대로 두고
누가 그 자리를 갖는지만 바꾼다(`moveTo`). 사이 값을 끼워 넣는 방식은 값이
점점 촘촘해지다 결국 끼울 자리가 없어진다. 바뀐 행만 갱신하므로 한 칸 옮기면
두 행, 멀리 보내면 그 사이 행들만 나간다(브라우저로 확인 — 6개 중 5번을 1번으로
보내면 5건, 한 칸 내리면 2건).

### PDF 페이지당 문제 수

`ExportComposer`의 `slotsFor()`. 1개(왼쪽에 문제·오른쪽은 풀이 공간, 기존
동작) / 2개(왼쪽 단과 오른쪽 단에 하나씩 나란히) / 4개(양쪽 2단, 위아래로도
나눠 넷, 좌상→좌하→우상→우하 순). 탐구처럼 문제가 짧으면 1개는 종이가 너무 남는다는 의견이 있어 넣었다.

### 실모 제목과 문제 번호는 손으로 고친다

- **실모 제목**(출처)은 상세 화면에서 바로 고친다(`CategoryTitleEditor`). 만들 때
  적은 출처가 곧 제목인데 오타를 내면 되돌릴 방법이 없었다. 점수는 여기서
  건드리지 않는다 — 화면에 붙어 보이지만 따로 관리되는 값이라 같이 고치면
  무엇을 바꾸는 중인지 헷갈린다.
- **문제 번호**를 직접 적을 수 있다. 실제 시험지의 번호를 그대로 쓰고 싶을 때가
  있다(15·22·28처럼 띄엄띄엄). 수정 창의 "문제 번호" 칸에 적고, 비워 두면
  예전대로 본문 맨 앞에서 뽑거나(`parseProblemNumber`) 차례대로 1번부터 매긴다.
  **손으로 적은 값이 무엇보다 우선한다**(`numberFor`) — 통째로 그린 문제는 본문이
  없어서 뽑을 것도 없다.
  저장 위치는 `problems.box_range.number` 다. 글자 크기·그림과 같은 이유로
  새 컬럼을 만들지 않았다(마이그레이션을 안 돌린 사람에게는 저장 자체가 실패한다).
  목록에서는 `problemNo:box_range->number` 로 그 키만 뽑아 온다.

### 정답 입력 — 유형을 고르지 않는다

`AnswerInput`. 예전에는 "객관식/주관식"을 먼저 고르게 했는데 군더더기였다.
지금은 **어디에 입력하느냐가 곧 유형이다** — ①②③④⑤ 버튼을 누르면 객관식,
아래 칸에 쓰면 주관식. 저장되는 값은 여전히 원문("3")이고 원숫자 변환은
표시할 때만 한다.

### 과거에 해결한 버그 (재발 시 참고)

- Mathpix가 `latex_styled`만 주거나 `\( \)` / `\[ \]` 델리미터를 쓰면 LaTeX가 그대로
  글자로 보이던 문제 → `renderMathText`에서 델리미터 정규화 + 델리미터 없는 순수
  LaTeX는 통째로 블록 수식 처리
- 아이폰 사진(수천 px)을 무압축 PNG data URL로 보내다 Safari에서
  "The string did not match the expected pattern." 발생 → `src/lib/cropImage.ts`에서
  긴 변 1600px 이하 + JPEG 품질 0.9로 압축해 전송
- HEIC/HEIF 업로드 시 안내 메시지 표시
- `main` 브랜치가 원격에서 통째로 삭제된 적이 있음(원인 불명). 커밋은 GitHub에
  GC되지 않고 SHA로 남아 있어서 `git fetch origin <sha>`로 복구 가능했음. 브랜치가
  안 보이면 먼저 Vercel 배포 메타데이터의 `githubCommitSha`로 마지막 커밋을 찾아볼 것.

### 평가원 문제지 양식으로 내보내기 (PDF)

수능 문제지 판형에 오답을 붙여 PDF 로 내보낸다. `ExportComposer` 의 "양식"
토글에서 고른다(`KiceExportPanel`, `next/dynamic` 으로 지연 로딩 — 틀 좌표와
글꼴 다섯 벌을 일반 A4 사용자에게까지 받게 할 이유가 없다).

**한때 hwpx(한글 파일)로 내보냈다.** 이유는 딱 하나 글꼴이었다 — 수능 문제지는
한컴 전용 글꼴(HFT)로 짜여 있어 웹에 심을 수 없으니, 글자를 한 자도 우리가
그리지 않고 원본 틀에 이미지만 얹은 뒤 PDF 로 뽑는 마지막 한 걸음을 사용자가
한글에서 하게 했다. 지금은 **같은 이름의 TTF 를 손에 넣어** 우리가 직접 그린다
(신그래픽체·(환)견명조·(환)태고딕·(환)디나루·(한)신중명조). 한글이 없어도 되고
받자마자 인쇄할 수 있다. hwpx 를 만들던 코드는 지웠다(`src/lib/hwpx/frame.ts`,
`text.ts` — 필요하면 git 이력에서 찾을 것).

**글꼴 파일은 저장소에 없다.** 배포권이 우리에게 없다. Supabase 비공개 버킷
`kice-fonts`(마이그레이션 `0015`)에 올려 두고 로그인한 사용자만
`/api/kice/font/[file]` 로 받아 간다. 넣는 법:

```bash
NEXT_PUBLIC_SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=... \
  node scripts/upload-kice-fonts.mjs <글꼴 폴더>
```

**미리 잘라서(pyftsubset) 올릴 것** — 원본은 한 벌에 수 MB라 다섯 벌을 받는
데만 한참 걸린다. KS X 1001 한글 2350자 + 라틴/기호면 다섯 벌 합쳐 2MB 아래다.
그리고 pdf-lib 에는 **`subset: false` 로 통째로** 넣어야 한다 —
pdf-lib(fontkit)의 서브셋터에 맡기면 **글자가 대부분 빈칸으로 찍히는데 오류는
나지 않는다.** 그 증상 때문에 한참 헤맸다(cmap 도 글리프 윤곽도 멀쩡했고
`subset: false` 로 바꾸는 순간 정상이 됐다).

#### 틀은 원본 좌표를 그대로 재생한다

제목 표·성명칸·둥근 교시 딱지·쪽번호 사선 상자는 눈대중으로 다시 그릴 수 있는
물건이 아니다. 원본이 조판해 둔 좌표를 뽑아 `public/kice/frames.json` 에 넣고
런타임에는 재생만 한다(`src/lib/kice/pdf.ts`). 만드는 순서는 **한 번만** 돌리고
결과를 커밋한다:

1. `node scripts/build-kice-frames.mjs <탐구 원본.hwpx> <수학 원본.hwpx>`
   → `public/kice/{tamgu,math}.hwpx` — 실제 수능 문항을 걷어내고 틀만 남긴다.
   원본을 그대로 커밋하면 저작물을 통째로 배포하는 셈이다(340KB/758KB → 각 20KB).
   만든 사람 이름·기기 이름 같은 문서 정보도 이때 지운다(`scrubMetadata`).
2. 그 틀에 빈 쪽 둘을 덧붙여 3쪽짜리로 만들고 rhwp 로 SVG 를 뽑는다.
3. `node scripts/kice/build-frames.mjs <탐구 SVG 폴더> <수학 SVG 폴더>`
   → `public/kice/frames.json` (첫 쪽 / 짝수 쪽 / 홀수 쪽 × 탐구 / 수학, 여섯 벌)

rhwp: <https://github.com/edwardkim/rhwp> (MIT, Rust). `cargo build --release
--bin rhwp` 로 9분쯤 걸려 빌드된다. **한컴 글꼴을 담지 않으므로**(`ttfs/FONTS.md`)
SVG 의 글꼴 **이름만** 믿고 모양은 우리가 심은 TTF 로 그린다.

**추출은 문서 순서대로 훑어야 한다.** 원본은 세로 구분선을 먼저 긋고 그 위를
흰 덮개로 가린다. 종류별로 몰아서 뽑으면 순서가 뒤집혀 **표지 센터라인이 위까지
길어진다**(`extract-frame.mjs`).

#### 손으로 다듬은 것들 (전부 실제 문제지와 대조해 고쳤다)

- **`제 N 교시` 딱지는 실제 문제지에서 오려낸 그림으로 덮는다**
  (`public/kice/period-{tamgu,math}.png`). 글꼴로 다시 그려서는 모서리의 둥근
  정도·글자 사이·숫자 크기가 조금씩 어긋났고 고칠 때마다 다른 데가 틀어졌다.
  과목이 달라도 숫자 하나만 바뀌는 고정 표시라 원본을 얹는 편이 정확하다.
- **원본이 적어 둔 글자 폭(`textLength`)은 쓰지 않는다.** 그 값은 한컴 글꼴
  메트릭으로 계산된 것이라 우리 TTF 에 강제로 맞추면 글자마다 눌린 정도가
  달라진다 — `제 4 교시` 에서 숫자만 15% 넓어져 혼자 커 보였다. 장평(`sx`)만 쓴다.
- **글자 수가 같은 갈아끼우기는 자리마다 한 자씩 넣는다.** `사회탐구 영역` →
  `과학탐구 영역` 을 한 덩어리로 다시 그려 가운데 맞추면 원본이 잡아 둔 글자
  자리를 통째로 벗어난다. 다만 **괄호 안 과목명은 한 덩어리로** 그린다 —
  `(사회·문화)` 의 가운뎃점 자리에 로마숫자 `Ⅰ` 같은 가는 글자가 들어가면
  앞뒤가 휑하게 벌어진다.
- **공백은 그리지 않는다.** 원본이 전각 공백(U+3000)을 쓰는 자리가 있는데 그대로
  그리면 글꼴에 없어서 **네모(⊠)가 찍힌다**(머리말에서 실제로 났다).
- 머리말 조각(영역명·쪽번호·과목명)은 원본이 4~5pt 씩 어긋난 줄에 얹어 두었다.
  가장 많은 글자가 놓인 줄에 나머지를 맞춘다(`alignHeaderBaseline`).
  **가로줄과의 간격은 원본 그대로 둔다** — 자리만 맞추는 것이지 벌리는 게 아니다.
- 쪽번호는 원본이 33pt 로 혼자 크고 견명조(볼드처럼 보인다)다. 영역명과 같은
  글꼴·같은 크기로 맞춘다(`matchPageNumberSize`). 이때 **`standalone` 표를 꼭
  붙여야 한다** — 묶는 기준이 (같은 줄·같은 크기·같은 글꼴)이라 셋이 같아지는
  순간 영역명과 한 덩어리가 되어 줄 글자가 `2사회탐구영역` 이 되고 갈아끼우기가
  통째로 빗나간다(실제로 영역명이 안 바뀌고 원본 그대로 찍혔다).
- 줄어든 만큼 **오른쪽에 붙는 쪽번호는 밀어 준다**(홀수 쪽). 왼쪽 시작점을
  그대로 두면 오른쪽 여백선에서 3pt 쯤 안으로 들어온다.
- 수학 문제지의 쪽번호 상자는 탐구 것과 미묘하게 다르다(테두리가 굵고 숫자가
  안쪽으로 들어와 있다). 모양은 탐구 것을 복사하되 **높이는 원래 자리를 지킨다**
  — 두 문제지는 본문이 끝나는 높이가 다르다(`copyBottomBox`).
- 세로 구분선이 머리말 글자를 관통하지 않게 가로줄 아래에서 시작시킨다
  (수학은 42pt 부터 그어져 있어 `수학 영역` 한가운데를 갈랐다).
- 표지 가로줄은 성명 칸 아래선에서 9pt 떨어뜨린다(원본 파일은 53pt 로 벌어져
  있었다). **가로줄만 옮기면 안 되고 그 위를 덮는 흰 덮개도 같이 줄여야 한다.**
- `(사회·문화)` 의 가운뎃점은 글자가 아니라 작은 원으로 따로 그려져 있다.
  과목명을 통째로 갈아끼우므로 그 점만 남아 새 과목명 한복판에 떠 있게 된다 — 지운다.

#### 쪽마다 몇 문제를 넣을지

흘러가는 대로 채우면 판형이 아무리 같아도 문제지처럼 보이지 않는다. 실제 탐구
문제지는 20문항을 **4·6·6·4** 로 나눠 싣는다. 그래서 쪽마다 개수를 정해 두고
그대로 짠다(`pagePattern`, 모자라면 처음으로 돌아가 되풀이한다). 화면에서
`탐구 기본 (4·6·6·4)` / `한 쪽에 1개` / `한 단에 N개` 를 고른다 — 한 쪽이 두
단이라 `한 단에 N개` 는 쪽당 그 두 배다. `한 쪽에 1개` 는 왼쪽 단에 문제,
오른쪽 단은 통째로 풀이 공간이 된다(A4 양식의 `1개` 와 같은 뜻).

**정해진 개수는 반드시 그 쪽에 들어간다**(`fitColumn`). 남는 자리는 문제 사이에
고르게 나눠 단을 꽉 채우고, 모자라면 그림을 줄여서 넣는다 — 다음 쪽으로 미루면
쪽마다 정해 둔 문항 수가 무너진다.

**출처 표기는 기본이 꺼짐이다.** 실제 문제지에 없는 글자라서다. 켜면 문제 위에
작게 붙는다.

**정답표는 맨 뒤에 한 쪽 붙는다**(기본 켜짐). 실제 문제지에는 없는 쪽이지만
이건 문제지가 아니라 **오답프린트**다 — 풀고 나서 맞춰 볼 답이 없으면 쓸모가
반이다. 정답을 적어 둔 문제만 표에 들어간다.

- 정답표 쪽도 **전체 쪽수에 포함**된다(쪽번호 상자에 그대로 찍힌다).
- 그 쪽에는 **단 구분선을 긋지 않는다** — 표를 세로로 관통해 버린다. 머리말과
  쪽번호는 그대로 둬서 앞쪽들과 한 묶음으로 보이게 한다. 본문 위아래 끝은
  구분선을 빼기 **전의** 틀에서 잰다(뺀 틀에는 아래 끝이 없다).
- 표 폭은 벌마다 고정(150pt)이고 가운데 정렬이다. 종이 폭을 다 늘리면 표가
  아니라 줄자처럼 보인다. 답이 많으면 벌 수를 늘려 옆으로 벌린다(48개 → 4벌).

#### 카드에 구워진 테두리

인식 결과 화면의 카드에는 둥근 모서리·회색 테두리·옅은 그림자가 걸려 있는데,
그건 화면 장치일 뿐인데도 **캡처하면 저장된 PNG 에 구워진다.** 그러면 어떤
양식으로 인쇄하든 따라다니고, 평가원 양식에서는 실제 문제지에 없는 네모가
문항마다 생긴다. `CARD_CAPTURE_OPTIONS`(`src/lib/layout.ts`)가 캡처할 때만
테두리를 투명하게 만든다.

**지우지 않고 투명하게만 한다.** `border: none` 으로 지우면 좌우 1px 씩이 본문
폭으로 돌아가 줄바꿈이 달라질 수 있다 — 카드 폭을 못 박아 둔 이유가 바로
그것이다. 실제 브라우저로 확인했다: 테두리 픽셀 `226,232,240` → `255,255,255`,
캡처 크기와 본문 폭은 그대로(1280×174, 574px).

**이미 저장된 문제에는 테두리가 이미 구워져 있다.** 그래서 내보낼 때 한 번 더
지운다(`stripCardBorder`) — 가장자리를 흰 띠로 덮는다. 잘라내지 않는 이유는
**둥근 모서리** 때문이다. 테두리 두께만큼만 잘라내면 네 귀퉁이의 굽은 자국이
남는다. 띠 폭은 카드 안쪽 여백(`p-8`, 32 CSS px)의 절반쯤이라 본문을 건드리지
않고, **테두리가 있을 때만** 덮는다(아무 이미지에나 두르면 가장자리 내용이
날아간다). 브라우저로 확인 — 테두리 있는 카드는 `226,232,240` → `255,255,255`,
테두리 없는 카드는 크기·픽셀 그대로.

**틀 파일을 `force-cache` 로 받으면 안 된다.** 브라우저가 오래된 사본을 그대로
내주어서 틀을 고쳐 배포해도 쓰던 사람에게는 옛 문제지가 계속 나온다(수학
표지에서 없앤 칸이 계속 보였다). `no-cache` 는 캐시를 쓰되 매번 확인은 한다 —
안 바뀌었으면 304 라 26KB 를 다시 받지 않는다.

#### 쪽번호와 전체 쪽수

쪽마다 값이 달라지므로 **글자로 짝지어 찾을 수 없다**(2쪽의 `2` 와 전체 쪽수
`20` 을 글자로 구분할 방법이 없다). 틀을 만들 때 `role: "pageNo" | "pageTotal"`
표를 붙여 두고 그리는 쪽에서 실제 값을 넣는다. 여러 자리 숫자를 위해
`alignRight`(자릿수가 늘어나도 오른쪽 끝이 그대로여야 하는 자리)도 함께 적어 둔다.

전체 쪽수는 마지막 쪽까지 짜 봐야 아는 값이라 **두 번 훑는다** — `layoutPages`
가 그림을 단에 흘려 넣어 쪽을 미리 짜고, 그 쪽수를 들고 다시 그린다.
그래서 `frameBounds`(머리말 아래 가로줄 = 본문 시작, 단 구분선 끝 = 본문 끝)는
그리는 쪽과 **같은 규칙**이어야 한다. 어긋나면 쪽수가 틀린다.

#### 수학 표지에는 성명·수험번호 칸이 없다

탐구 표지에는 있는데 수학에는 없다. 원본 hwpx 를 걷어낼 때 떨어져 나간 줄 알고
탐구 것을 옮겨 심어 봤다가(`copyNameRow`, git 이력에 있다) **없는 게 맞다**고
확인해 되돌렸다. 대신 그 높이만큼 **세로 단 구분선이 위로 더 올라와** 머리말
가로줄(168.65pt)에 바로 닿는다 — 칸이 없어도 간격이 비지 않는 이유다.

## 남은 작업 (여기서부터 이어서)

1. **DB 마이그레이션 재확인** — `supabase/migrations/0001_init.sql`(categories/problems/
   problem-images 버킷)이 실제 `bmhupmkxzvbqndxkvjmx` 프로젝트에 적용됐는지 확인 필요.
   이전에 사용자에게 실수로 다른(지금은 버려진) 스키마인 `problem_history` 테이블
   SQL을 먼저 전달한 적이 있어서, `0001_init.sql`을 아직 안 돌렸을 수 있음.
   **`0009_diagram_credit_amount.sql`도 아직 실행 안 됐을 수 있음** — 이게 없으면
   "도형 추가인식" 버튼이 크레딧 차감에 실패해 500 에러가 남(RPC가 `p_amount`
   인자를 못 받는 옛 함수로 남아있기 때문).
   **`0010_diagram_model_quota.sql`은 반드시 실행해야 함** — 도형 모델 선택/
   결제자 전용/플래시쿠폰이 전부 이 파일의 함수에 의존한다. 안 돌리면
   `consume_diagram_credit` 함수가 없어서 도형 추가인식이 500으로 죽는다.
2. **이메일 확인 리디렉션 URL 등록** — Supabase 대시보드 Authentication > URL
   Configuration에 `{mathocr 배포 도메인}/auth/callback` 추가.
3. **Vercel 환경변수 확인** — `mathocr` 프로젝트(배포에 실제 쓰이는 프로젝트)에
   `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`,
   `MATHPIX_APP_ID`, `MATHPIX_APP_KEY`가 들어있는지 확인.
   `GEMINI_API_KEY`(도형 SVG 재구성용)는 선택 사항 — 없어도 원본 크롭
   이미지로 대체 표시되니 앱이 죽지 않음. `NVIDIA_API_KEY`는 이제 안 씀.
4. **실제 동작 검증** — 회원가입 → 이메일 확인 → 로그인 → 실모추가 → 오답추가 →
   PDF 내보내기 전 과정.
5. **평가원 양식 글꼴 올리기** — `0015_kice_fonts_bucket.sql` 을 돌린 뒤
   `node scripts/upload-kice-fonts.mjs <글꼴 폴더>` 로 다섯 벌을 올려야 한다.
   안 올리면 "평가원 양식 PDF 만들기"가 404 로 실패한다(그 사실을 화면에 그대로
   알려 준다). 파일은 저작권 때문에 저장소에 없다.

### 문제 카드는 축소해서 보여준다 (ScaledCard)

카드 너비(`PROBLEM_CARD_WIDTH`)는 어떤 기기에서든 같아야 한다 — 그래야 결과물이
같다. 그런데 휴대폰 화면은 그보다 좁아서 예전에는 가로로 밀어 봐야 했다.
`ScaledCard`가 **바깥 껍데기에만** `transform: scale()`을 걸어 통째로 줄인다.

- **축소를 카드 자체에 걸면 안 된다.** 캡처 대상(`cardRef`)에 transform이 걸리면
  저장되는 PNG가 같이 줄거나 잘릴 수 있다. 껍데기에만 걸어 캡처는 원래 크기를
  유지한다. 실제 브라우저에서 확인 — 390 / 700 / 1200px 폭 전부에서 캡처가
  1520×880으로 동일했고 카드는 화면에 정확히 들어맞았다.
- transform은 레이아웃 크기를 바꾸지 않아서(줄여도 원래 자리를 차지한다) 바깥
  높이를 직접 계산해 준다. 안 그러면 카드 아래에 빈 공간이 잔뜩 남는다.
- **드래그 계산은 배율로 나눠야 한다.** 손가락이 움직인 화면 거리와 카드 안의
  거리가 다르다(`scaleRef`). 안내선 위치도 화면 좌표를 배율로 나눠 카드
  좌표계로 되돌린다.

`ResultStage`(인식 결과)와 `ProblemGallery`(수정 모달) 두 곳에서 쓴다.

### 도형·자료를 놓을 수 있는 자리 (anchors)

문단 사이뿐 아니라 **조건 박스·보기 박스 안의 줄 사이**도 자리가 된다.
문제집에서는 자료가 박스 안에 들어가는 경우가 흔하다.

그러려면 박스가 통짜 HTML이면 안 된다. `renderMathTextWithInfo`가 돌려주는
`blocks`는 `RenderedBlock[]`이고, 박스는 `{ kind: "box", lines: string[] }`로
줄 배열을 그대로 들고 있는다(`blockToHtml()`로 합칠 수 있다).

`ResultStage`의 `anchors`가 자리 목록이고, `figurePos[id]`는 그 배열의
인덱스다. `line`이 null이면 그 블록 앞(박스라면 테두리 **바깥**), 숫자면 박스
안 그 줄 앞이다(`lines.length`면 박스 안 맨 끝).

**세 곳이 정확히 같은 순서로 훑어야 한다** — `anchors` 생성, `cardHtml` 조립,
드래그의 `anchorPoints()`. 하나라도 어긋나면 그림이 엉뚱한 자리에 붙는다.
드래그 판정은 "가장 가까운 자리"로 한다(자리마다 y를 재서 최솟값).

### 모델에 보내기 전에 대비를 올린다 (autoContrast)

문제집을 손으로 찍은 사진은 종이가 회색으로 나오고 글자가 옅다(실내 조명,
그림자, 자동 노출). 사람 눈에는 읽히지만 모델에게는 글자와 종이의 차이가 작아서
못 읽은 글자를 지어내거나 아예 실패한다. `enhanceContrast()`가 **보내기 직전에**
한 번 편다 — Mathpix 인식, 문제 전체 다시 그리기, 그림 하나 다시 그리기, 문제
영역 찾기 넷 다 이 길을 지난다.

**화면에 남는 원본은 건드리지 않는다.** 도형을 오려낼 때 원래 픽셀이 필요하고,
저장되는 결과물이 사진 보정을 탄 것처럼 보이면 안 된다.

하는 일은 히스토그램 늘리기 하나다(위아래 0.5%를 잘라 내고 그 사이를 편다).
**일부러 하지 않는 것들:**

- 흑백으로 바꾸지 않는다 — 사회탐구 지도·그래프는 색이 뜻을 가진다.
- 이진화하지 않는다 — 가는 점선·연한 빗금이 통째로 사라진다.
- **채널마다 같은 값을 더하고 빼지 않는다.** 그러면 옅은 색이 원색으로 튄다 —
  실제로 옅은 빨강 `(180,120,110)`이 `(184,0,0)`이 되어 지도의 파스텔 구분이
  통째로 날아갔다. 대신 **밝기의 배율**을 구해 세 채널에 똑같이 곱한다.
- **가장 어두운 것을 무조건 검정으로 만들지 않는다**(`BLACK_CAP`). 글자가 없고
  옅은 색면만 있는 자료에서는 그 색면이 "가장 어두운 것"이라, 그대로 늘리면
  지역이 통째로 새까매진다(옅은 빨강·파랑이 둘 다 검정이 됐다).
- 이미 대비가 충분하거나(스캔본) 거의 단색이면 손대지 않는다.

브라우저로 확인 — 흐린 사진 94~193 → 2~255, 색칠된 지도는 색조를 지킨 채
종이만 흰색으로, 또렷한 스캔본과 거의 빈 그림은 **한 픽셀도 바뀌지 않는다.**

### 이미지 생성 결과의 빈 여백

이미지 생성 모델은 자료가 가로로 길든 세로로 길든 자기 비율(대개 정사각형)에
맞춰 그려서 둘레에 흰 여백을 잔뜩 붙여 돌려준다. 그대로 끼우면 그림보다 여백이
더 커져 문단 사이가 휑하게 벌어진다. `trimBlankBorder()`가 내용이 있는 부분만
남기고 잘라낸다(1024×1024 정사각형에 든 가로형 도식 기준 세로 여백 66% 제거).
이미 꽉 찬 그림이나 완전히 빈 이미지는 원본을 그대로 돌려준다(불필요한 재인코딩 방지).

## 주의할 점

- **Vercel 프로젝트 `mathpix`의 Production Branch는 `main`을 따라가지 않는다.**
  실제 서비스 중인 배포는 `mathocr` 프로젝트 쪽이며, 그쪽은 Production Branch가
  `main`으로 정상 연결되어 있어 보통 `main`에 push하면 자동으로 재배포된다.
- **2026-07-27 세션에서 GitHub→Vercel 웹훅이 멈춘 적 있음** — 커밋 3개(`3d19299`,
  `16e4825`, `7127b54`)를 연달아 `main`에 push했는데 `mathocr` 프로젝트에 배포가
  하나도 안 생김(재배포조차 안 됨, 30분+ 대기해도 동일). `list_deployments`로
  최신 배포의 `githubCommitSha`가 push한 커밋과 같은지 꼭 확인할 것 — 다를 경우
  Vercel 대시보드 Settings→Git 연결 상태나 GitHub Settings→Webhooks 배송 로그를
  사용자에게 확인받아야 함(API로는 재연결 불가). 이 문제 때문에 도형 재구성
  모델을 90b로 올린 커밋이 한동안 실제 서비스에 반영되지 않았었다.
- `main`에 직접 push하는 것이 이 저장소의 관행이었음(PR 없이). 다만 세션 지침에서
  특정 작업 브랜치를 지정한 경우 그 브랜치에서 작업하고, `main`에 반영할 때는
  사용자에게 확인받을 것.
- 이전 세션에서 중복 스캐폴드 브랜치 1개(`claude/math-problem-image-recognition-64hjax`)가
  원격에 남아 있음. 내용은 `main`에 모두 반영돼 있으니 무시해도 된다.
- 사용자가 채팅에 API 키를 평문으로 붙여넣은 적 있음(Mathpix, Supabase anon key 등).
  민감정보를 다룰 때 채팅에 노출하지 말 것.

## 명령어

```bash
npm install
npm run dev     # http://localhost:3000
npm run build   # 배포 전 항상 확인
```
